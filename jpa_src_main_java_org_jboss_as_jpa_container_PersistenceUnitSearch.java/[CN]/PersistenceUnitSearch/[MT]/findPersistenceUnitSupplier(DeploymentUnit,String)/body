{
  PersistenceUnitMetadata name=findWithinModule(deploymentUnit,persistenceUnitName,true);
  if (name == null)   name=findWithinApplication(getTopLevel(deploymentUnit),persistenceUnitName);
  return name;
}
