{
  Entry object;
synchronized (cache) {
    object=cache.remove(new ArrayKey(key));
  }
  if (object == null)   throw new NoSuchEJBException(String.valueOf(key));
  factory.destroyInstance(object.value);
}
