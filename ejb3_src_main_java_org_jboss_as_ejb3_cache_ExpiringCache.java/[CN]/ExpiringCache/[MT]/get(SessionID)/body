{
synchronized (cache) {
    Entry val=cache.get(key);
    if (val == null) {
      return null;
    }
    val.lastUsed=System.currentTimeMillis();
    val.state=State.IN_USE;
    return val.getValue();
  }
}
