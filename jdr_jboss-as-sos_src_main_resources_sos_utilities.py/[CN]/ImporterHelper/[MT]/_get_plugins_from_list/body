def _get_plugins_from_list(self, list_, package_path=None):
    if (not package_path):
        package_path = self.package_path
    plugins = [self._plugin_name(plugin) for plugin in list_ if ((package_path in plugin) and ('__init__' not in plugin) and plugin.endswith('.py'))]
    plugins.sort()
    return plugins
