def _find_plugins_in_dir(self, path):
    candidate = os.path.join(path, self.package_path)
    if os.path.exists(candidate):
        pnames = self._get_plugins_from_list(list(find('*.py', candidate)))
        if pnames:
            return pnames
