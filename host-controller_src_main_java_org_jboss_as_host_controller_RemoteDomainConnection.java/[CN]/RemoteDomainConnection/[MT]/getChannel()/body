{
  Channel result;
  if (!closing) {
    result=awaitChannel();
  }
 else {
    result=super.getChannel();
    if (result == null) {
      throw ProtocolMessages.MESSAGES.channelClosed();
    }
  }
  return result;
}
