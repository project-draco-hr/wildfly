def _is_not_default(self, plugin_name, pluginClass):
    return ((not pluginClass(self.get_commons()).defaultenabled()) and (not (plugin_name in self.opts.enableplugins)) and (not (plugin_name in self.opts.onlyplugins)))
