def batch(self):
    if self.opts.batch:
        self.ui_log.info(self.msg)
    else:
        self.msg += _('Press ENTER to continue, or CTRL-C to quit.\n')
        try:
            raw_input(self.msg)
        except:
            self.ui_log.info('')
            self._exit()
