def _is_inactive(self, plugin_name, pluginClass):
    return ((not pluginClass(self.get_commons()).checkenabled()) and (not (plugin_name in self.opts.enableplugins)) and (not (plugin_name in self.opts.onlyplugins)))
