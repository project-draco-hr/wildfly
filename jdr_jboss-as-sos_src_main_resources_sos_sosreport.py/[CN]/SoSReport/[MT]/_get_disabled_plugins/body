def _get_disabled_plugins(self):
    disabled = []
    if self.config.has_option('plugins', 'disable'):
        disabled = [plugin.strip() for plugin in self.config.get('plugins', 'disable').split(',')]
    return disabled
