def clean(self):
    for (fname, f) in self.files:
        try:
            f.flush()
            f.close()
        except Exception as e:
            pass
        try:
            os.unlink(fname)
        except Exception as e:
            pass
    self.files = []
