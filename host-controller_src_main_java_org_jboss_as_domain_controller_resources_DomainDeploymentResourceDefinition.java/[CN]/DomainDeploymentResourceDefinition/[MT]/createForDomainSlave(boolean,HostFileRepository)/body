{
  return new DomainDeploymentResourceDefinition(DeploymentResourceParent.DOMAIN,DeploymentAttributes.DOMAIN_DEPLOYMENT_ADD_DEFINITION,new DeploymentAddHandler(backupDC ? fileRepository : null),DeploymentRemoveHandler.createForSlave(fileRepository));
}
