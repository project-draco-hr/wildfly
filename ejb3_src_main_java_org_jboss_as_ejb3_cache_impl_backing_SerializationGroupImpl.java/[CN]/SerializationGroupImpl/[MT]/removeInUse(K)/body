{
  if (inUseKeys.remove(key)) {
    decreaseUsageCount();
    if (inUseKeys.size() != 0)     setLastUsed(System.currentTimeMillis());
  }
 else   if (!this.memberObjects.containsKey(key)) {
    throw EjbMessages.MESSAGES.missingSerializationGroupMember(key,this);
  }
}
