{
  final ServerSecurityManager securityManager=securityManagerValue.getOptionalValue();
  if (securityManager == null)   throw EjbLogger.ROOT_LOGGER.securityNotEnabled();
  return securityManager;
}
