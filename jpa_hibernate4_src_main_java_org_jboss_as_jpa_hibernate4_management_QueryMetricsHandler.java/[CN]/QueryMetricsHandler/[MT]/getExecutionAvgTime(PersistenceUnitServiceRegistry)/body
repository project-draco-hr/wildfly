{
  return new QueryMetricsHandler(persistenceUnitRegistry){
    @Override protected void handle(    QueryStatistics statistics,    OperationContext context,    String attributeName,    String originalQueryName){
      long count=statistics.getExecutionAvgTime();
      context.getResult().set(count);
    }
  }
;
}
