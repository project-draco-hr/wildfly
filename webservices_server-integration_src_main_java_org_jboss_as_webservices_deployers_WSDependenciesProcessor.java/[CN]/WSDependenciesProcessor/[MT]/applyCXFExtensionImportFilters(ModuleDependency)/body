{
  dep.getImportFilters().add(new FilterSpecification(PathFilters.match("META-INF/cxf"),true));
  dep.getImportFilters().add(new FilterSpecification(PathFilters.match("META-INF/spring.*"),true));
  return dep;
}
