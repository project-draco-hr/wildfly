{
  if (getSecurityManager() == null) {
    object.setAccessible(true);
  }
 else {
    doPrivileged(new PrivilegedAction<Object>(){
      public Object run(){
        object.setAccessible(true);
        return null;
      }
    }
);
  }
}
