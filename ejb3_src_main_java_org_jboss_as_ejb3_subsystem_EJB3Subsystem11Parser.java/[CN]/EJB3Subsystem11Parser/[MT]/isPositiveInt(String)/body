{
  if (val == null || val.trim().isEmpty()) {
    return false;
  }
  try {
    final Integer value=Integer.parseInt(val);
    if (value <= 0) {
      return false;
    }
  }
 catch (  NumberFormatException nfe) {
    return false;
  }
  return true;
}
