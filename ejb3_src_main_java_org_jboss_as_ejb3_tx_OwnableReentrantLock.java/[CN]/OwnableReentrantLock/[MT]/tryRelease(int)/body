{
  int c=getState() - releases;
  if (!currentRequestor().equals(owner)) {
    return false;
  }
  boolean free=false;
  if (c == 0) {
    free=true;
    owner=null;
  }
  setState(c);
  return free;
}
