{
  if (_nc == null)   throw JacORBMessages.MESSAGES.notANamingContext(nc.toString());
  try {
    nc.destroy();
  }
 catch (  Exception e) {
    throw org.jboss.as.jacorb.naming.jndi.ExceptionMapper.mapException(e,this,null);
  }
}
