{
  if (_nc == null)   throw new ConfigurationException("Context does not have a corresponding NamingContext");
  if (oldName.size() == 0 || newName.size() == 0)   throw new InvalidNameException("One or both names empty");
  java.lang.Object obj=lookup(oldName);
  bind(newName,obj);
  unbind(oldName);
}
