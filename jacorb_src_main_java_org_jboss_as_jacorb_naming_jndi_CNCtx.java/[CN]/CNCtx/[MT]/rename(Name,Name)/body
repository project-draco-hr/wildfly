{
  if (_nc == null)   throw JacORBLogger.ROOT_LOGGER.notANamingContext(oldName.toString());
  if (oldName.size() == 0 || newName.size() == 0)   throw JacORBLogger.ROOT_LOGGER.invalidEmptyName();
  java.lang.Object obj=lookup(oldName);
  bind(newName,obj);
  unbind(oldName);
}
