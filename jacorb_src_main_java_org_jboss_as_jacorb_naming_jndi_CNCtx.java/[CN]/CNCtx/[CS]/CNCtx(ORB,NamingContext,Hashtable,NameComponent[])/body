{
  if (orb == null || nctx == null)   throw JacORBLogger.ROOT_LOGGER.errorConstructingCNCtx();
  _orb=orb;
  _nc=nctx;
  _env=env;
  _name=name;
  federation="true".equals(env != null ? env.get(FED_PROP) : null);
}
