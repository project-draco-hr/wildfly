{
  TestClass testCase=context.get(TestClass.class);
  if (OSGiTestEnricher.isInjectionTarget(testCase.getJavaClass()))   return true;
  Manifest manifest=getOrCreateManifest(appArchive);
  if (BundleInfo.isValidateBundleManifest(manifest))   return true;
  return false;
}
