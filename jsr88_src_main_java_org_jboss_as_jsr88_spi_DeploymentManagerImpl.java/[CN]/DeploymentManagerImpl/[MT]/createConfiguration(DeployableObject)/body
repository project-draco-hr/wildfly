{
  if (obj == null)   throw new IllegalArgumentException(MESSAGES.nullArgument("deployableObject"));
  if (obj.getType().equals(ModuleType.WAR))   return new WarConfiguration(obj);
  throw new InvalidModuleException(MESSAGES.moduleTypeNotSupported(obj.getType()));
}
