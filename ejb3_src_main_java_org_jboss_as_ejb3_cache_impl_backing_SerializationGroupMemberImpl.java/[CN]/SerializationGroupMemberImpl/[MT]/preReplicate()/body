{
  if (tryLock(2)) {
    try {
      setGroup(null);
      cache.notifyPreReplicate(this);
    }
  finally {
      unlock();
    }
  }
 else {
    throw EjbMessages.MESSAGES.cacheEntryInUse(this);
  }
}
