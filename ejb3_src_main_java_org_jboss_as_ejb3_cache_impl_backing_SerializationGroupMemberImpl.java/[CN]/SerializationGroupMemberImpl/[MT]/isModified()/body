{
  boolean localModified=(this.value != null && this.value.isModified());
  if (localModified && group != null)   group.setGroupModified(true);
  return (group == null ? localModified : group.isGroupModified());
}
