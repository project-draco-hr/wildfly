{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] getParameterMetaData()",jndiName,Constants.SPY_LOGGER_PREFIX_PREPARED_STATEMENT);
      return ps.getParameterMetaData();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
