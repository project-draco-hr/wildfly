{
  lock();
  try {
    checkTransaction();
    try {
      checkConfiguredQueryTimeout();
      if (spy)       spyLogger.debugf("%s [%s] execute()",jndiName,Constants.SPY_LOGGER_PREFIX_PREPARED_STATEMENT);
      return ps.execute();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
