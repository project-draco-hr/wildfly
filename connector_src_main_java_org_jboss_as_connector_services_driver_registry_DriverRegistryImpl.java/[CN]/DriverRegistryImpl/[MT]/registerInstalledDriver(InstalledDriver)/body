{
  if (driver == null)   throw new IllegalArgumentException(MESSAGES.nullVar("driver"));
  DEPLOYMENT_CONNECTOR_REGISTRY_LOGGER.tracef("Adding driver: %s",driver);
synchronized (drivers) {
    drivers.put(driver.getDriverName(),driver);
  }
}
