{
  Iterator<String> it=logs.iterator();
  int counter=0;
  while (it.hasNext()) {
    String log=(String)it.next();
    if (log.contains(substring)) {
      counter++;
      if (counter == 2) {
        return true;
      }
    }
  }
  return false;
}
