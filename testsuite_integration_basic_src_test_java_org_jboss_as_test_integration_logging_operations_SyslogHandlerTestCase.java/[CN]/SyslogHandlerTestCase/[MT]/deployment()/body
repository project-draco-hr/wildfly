{
  final WebArchive war=ShrinkWrap.create(WebArchive.class,"test.war");
  war.addClasses(LoggingServlet.class);
  war.setManifest(new StringAsset(Descriptors.create(ManifestDescriptor.class).attribute("Logging-Profile","syslog-profile").exportAsString()));
  return war;
}
