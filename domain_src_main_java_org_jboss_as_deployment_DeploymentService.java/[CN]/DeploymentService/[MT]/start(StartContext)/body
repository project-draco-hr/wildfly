{
  if (mountHandle == null) {
    try {
      if (deploymentRoot.isFile())       mountHandle=VFS.mountZip(deploymentRoot,deploymentRoot,TempFileProviderService.provider());
    }
 catch (    IOException e) {
      throw new StartException("Failed to mount deployment archive",e);
    }
  }
}
