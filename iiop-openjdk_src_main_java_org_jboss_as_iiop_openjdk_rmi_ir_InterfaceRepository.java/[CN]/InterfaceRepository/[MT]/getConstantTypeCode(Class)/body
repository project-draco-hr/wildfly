{
  if (cls == null)   throw IIOPMessages.MESSAGES.invalidNullClass();
  TypeCode ret=constantTypeCodeMap.get(cls);
  if (ret == null)   throw IIOPMessages.MESSAGES.badClassForConstant(cls.getName());
  return ret;
}
