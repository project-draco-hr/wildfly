{
  if (cls == null)   throw IIOPMessages.MESSAGES.invalidNullClass();
  TypeCode tc=(TypeCode)typeCodeMap.get(cls);
  if (tc != null)   throw IIOPMessages.MESSAGES.duplicateTypeCodeForClass(cls.getName());
  typeCodeMap.put(cls,typeCode);
}
