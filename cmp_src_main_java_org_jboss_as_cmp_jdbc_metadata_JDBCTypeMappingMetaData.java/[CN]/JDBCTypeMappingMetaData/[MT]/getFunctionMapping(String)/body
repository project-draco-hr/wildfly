{
  JDBCFunctionMappingMetaData funcMapping=(JDBCFunctionMappingMetaData)functionMappings.get(name.toLowerCase());
  if (funcMapping == null)   throw CmpMessages.MESSAGES.functionNotDefinedForType(name,this.name);
  return funcMapping;
}
