{
  try {
    ModelControllerClient client=ModelControllerClient.Factory.create(host,port,new RbacAdminCallbackHandler(username,password),SASL_OPTIONS);
    return new NativeManagementInterface(client);
  }
 catch (  UnknownHostException e) {
    throw new RuntimeException(e);
  }
}
