{
  IOException ex=null;
  try {
    pipe.getOut().close();
  }
 catch (  IOException e) {
    ex=e;
  }
  try {
    pipe.getIn().close();
  }
 catch (  IOException e) {
    ex=e;
  }
  if (ex != null) {
    throw ex;
  }
}
