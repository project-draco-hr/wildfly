{
  return context instanceof ServerOperationContextImpl && (!operation.hasDefined(OPERATION_HEADERS) || !operation.get(OPERATION_HEADERS).hasDefined(ROLLBACK_ON_RUNTIME_FAILURE) || operation.get(OPERATION_HEADERS,ROLLBACK_ON_RUNTIME_FAILURE).asBoolean());
}
