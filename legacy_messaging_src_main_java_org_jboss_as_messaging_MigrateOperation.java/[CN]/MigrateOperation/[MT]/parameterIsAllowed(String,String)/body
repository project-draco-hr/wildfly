{
switch (resourceType) {
case REMOTE_ACCEPTOR:
case HTTP_ACCEPTOR:
case REMOTE_CONNECTOR:
case HTTP_CONNECTOR:
    if ("use-nio".equals(name)) {
      return false;
    }
 else {
      return true;
    }
default :
  return true;
}
}
