{
  JPAService jpaService=new JPAService();
  JPAService.defaultDataSourceName=defaultDataSourceName;
  target.addService(SERVICE_NAME,jpaService).setInitialMode(ServiceController.Mode.ACTIVE).install();
}
