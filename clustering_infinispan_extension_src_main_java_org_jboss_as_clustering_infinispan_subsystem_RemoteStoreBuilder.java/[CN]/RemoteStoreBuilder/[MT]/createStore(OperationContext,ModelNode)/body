{
  this.storeBuilder=new ConfigurationBuilder().persistence().addStore(RemoteStoreConfigurationBuilder.class).remoteCacheName(CACHE.getDefinition().resolveModelAttribute(context,model).asString()).socketTimeout(SOCKET_TIMEOUT.getDefinition().resolveModelAttribute(context,model).asLong()).tcpNoDelay(TCP_NO_DELAY.getDefinition().resolveModelAttribute(context,model).asBoolean());
  for (  String binding : StringListAttributeDefinition.unwrapValue(context,SOCKET_BINDINGS.getDefinition().resolveModelAttribute(context,model))) {
    this.bindings.add(new CapabilityDependency<>(context,RequiredCapability.OUTBOUND_SOCKET_BINDING,binding,OutboundSocketBinding.class));
  }
  return this.storeBuilder;
}
