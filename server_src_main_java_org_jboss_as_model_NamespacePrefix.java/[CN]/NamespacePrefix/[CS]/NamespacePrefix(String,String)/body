{
  if (prefix == null) {
    throw new IllegalArgumentException("prefix is null");
  }
  this.prefix=prefix;
  if (uri == null) {
    throw new IllegalArgumentException("uri is null");
  }
  this.uri=uri;
}
