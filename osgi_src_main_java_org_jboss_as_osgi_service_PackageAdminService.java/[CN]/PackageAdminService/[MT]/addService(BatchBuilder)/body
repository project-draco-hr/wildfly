{
  PackageAdminService service=new PackageAdminService();
  batchBuilder.addService(PackageAdminService.SERVICE_NAME,service).addDependency(FrameworkService.SERVICE_NAME,BundleContext.class,service.injectedContext).setInitialMode(Mode.ON_DEMAND).install();
}
