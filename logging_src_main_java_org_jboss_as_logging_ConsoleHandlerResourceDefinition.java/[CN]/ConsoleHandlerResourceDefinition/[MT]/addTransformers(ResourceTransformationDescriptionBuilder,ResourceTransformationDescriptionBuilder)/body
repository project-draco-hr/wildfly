{
  final ResourceTransformationDescriptionBuilder child=subsystemBuilder.addChildResource(CONSOLE_HANDLER_PATH).getAttributeBuilder().addRejectCheck(RejectAttributeChecker.SIMPLE_EXPRESSIONS,AUTOFLUSH,TARGET).end();
  loggingProfileBuilder.discardChildResource(CONSOLE_HANDLER_PATH);
  return registerTransformers(child);
}
