{
  List list=new LinkedList();
  int token=st.nextToken();
  while (token != ']') {
    if (token == '{') {
      list.add(parseCompound(st));
    }
 else     if (token == '[') {
      list.add(parseList(st));
    }
 else {
      list.add(st.sval);
    }
    token=st.nextToken();
    if (token == ',') {
      token=st.nextToken();
    }
  }
  return list;
}
