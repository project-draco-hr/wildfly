{
  consoleOut=new ByteArrayOutputStream();
  System.setProperty("aesh.terminal","org.jboss.aesh.terminal.TestTerminal");
  ctx=CLITestUtil.getCommandContext(TestSuiteEnvironment.getServerAddress(),TestSuiteEnvironment.getServerPort(),createConsoleInput(),consoleOut);
  if (!connect) {
    return;
  }
  Assert.assertTrue(sendConnect(cliAddress));
}
