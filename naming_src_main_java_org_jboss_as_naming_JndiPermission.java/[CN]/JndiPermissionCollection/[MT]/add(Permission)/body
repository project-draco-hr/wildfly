{
  if (!(permission instanceof JndiPermission))   throw NamingLogger.ROOT_LOGGER.invalidPermission(permission);
  if (isReadOnly())   throw NamingLogger.ROOT_LOGGER.cannotAddToReadOnlyPermissionCollection();
synchronized (this) {
    perms.add((JndiPermission)permission);
  }
}
