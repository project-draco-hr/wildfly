{
  stopped=true;
  if (blocking)   handlerThread.interrupt();
  if (handlerThread.isAlive()) {
    try {
      handlerThread.join(100);
    }
 catch (    Exception ignored) {
    }
  }
  if (handlerThread.isAlive())   handlerThread.interrupt();
}
