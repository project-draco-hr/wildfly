{
  if (prefixNodeType == null)   throw new IllegalArgumentException("Can't accept node name '" + name + "' since the prefix doesn't appear to end on a node type");
  String nodeType=prefixNodeType;
  prefixNodeType=null;
  addNode(nodeType,name);
}
