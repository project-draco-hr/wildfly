{
  BundleContext syscontext=injectedSystemContext.getValue();
  if (activator != null) {
    try {
      activator.stop(syscontext);
      activator=null;
    }
 catch (    Exception e) {
      LOGGER.warn(e);
    }
  }
}
