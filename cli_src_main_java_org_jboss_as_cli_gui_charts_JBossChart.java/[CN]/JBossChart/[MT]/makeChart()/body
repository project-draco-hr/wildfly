{
  series=new TimeSeries(description);
  dataset=new TimeSeriesCollection(series);
  JFreeChart chart=ChartFactory.createTimeSeriesChart(name,"Time of Day",attrName,dataset,true,true,false);
  return new ChartPanel(chart);
}
