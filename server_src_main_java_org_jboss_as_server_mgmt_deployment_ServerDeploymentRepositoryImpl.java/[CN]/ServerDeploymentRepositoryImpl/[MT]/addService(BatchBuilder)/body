{
  ServerDeploymentRepositoryImpl service=new ServerDeploymentRepositoryImpl();
  batchBuilder.addService(SERVICE_NAME,service).addDependency(ServerEnvironmentService.SERVICE_NAME,ServerEnvironment.class,service.serverEnvironment).install();
}
