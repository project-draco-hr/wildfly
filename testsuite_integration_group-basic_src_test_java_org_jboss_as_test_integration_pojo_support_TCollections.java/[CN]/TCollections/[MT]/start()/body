{
  for (  Integer n : numbers)   System.out.println("n = " + n);
  for (  TBean b : beans)   System.out.println("b = " + b);
  for (  Map.Entry entry : map.entrySet())   System.out.println("k = " + entry.getKey() + ", v = "+ entry.getValue());
}
