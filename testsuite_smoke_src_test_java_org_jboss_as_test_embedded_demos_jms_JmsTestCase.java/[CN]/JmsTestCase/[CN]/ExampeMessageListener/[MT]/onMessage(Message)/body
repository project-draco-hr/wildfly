{
  TextMessage msg=(TextMessage)message;
  try {
    log.info("-----> on message: " + msg.getText());
    received=msg.getText();
    latch.countDown();
  }
 catch (  Throwable t) {
    t.printStackTrace();
  }
}
