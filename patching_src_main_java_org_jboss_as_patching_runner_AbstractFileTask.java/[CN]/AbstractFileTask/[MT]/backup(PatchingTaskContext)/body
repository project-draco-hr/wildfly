{
  if (target.isFile()) {
    final byte[] backupHash=IoUtils.copy(target,backup);
    return backupHash;
  }
 else   if (contentItem.isDirectory() && target.isDirectory()) {
    setIgnoreApply();
  }
  return NO_CONTENT;
}
