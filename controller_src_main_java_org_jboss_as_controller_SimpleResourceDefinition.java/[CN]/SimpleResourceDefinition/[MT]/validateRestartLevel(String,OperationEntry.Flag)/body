{
  if (flag != null && !RESTART_FLAGS.contains(flag)) {
    throw new IllegalArgumentException(String.format("%s is not a valid value for parameter" + " %s -- must be one of %s",flag,paramName,RESTART_FLAGS));
  }
  return flag;
}
