{
  PathUserService mine=new PathUserService();
  context.getServiceTarget().addService(SocketBindingUserService.NAME,mine).addDependency(AbstractPathService.pathNameOf("p2"),String.class,mine.pathValue).install();
}
