{
  final ZipFile zipFile=new ZipFile(zip);
  try {
    unpack(zipFile,patchDir);
  }
  finally {
    if (zip != null)     try {
      zipFile.close();
    }
 catch (    IOException ignore) {
    }
  }
}
