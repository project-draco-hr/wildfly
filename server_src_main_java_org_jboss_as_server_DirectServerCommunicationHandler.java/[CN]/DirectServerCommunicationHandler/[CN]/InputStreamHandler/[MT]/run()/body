{
  try {
    while (!shutdown.get()) {
      byte[] bytes=StreamUtils.readBytesWithLength(input);
      handler.handleMessage(bytes);
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
 finally {
    shutdown();
  }
}
