{
  if (plan == null)   throw new IllegalArgumentException("plan is null");
  if (setResults == null)   throw new IllegalArgumentException("setResults is null");
  this.plan=plan;
  this.setResults=setResults;
  this.idpe=null;
}
