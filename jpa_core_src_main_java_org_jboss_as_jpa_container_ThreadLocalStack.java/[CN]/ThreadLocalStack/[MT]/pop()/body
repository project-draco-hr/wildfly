{
  ArrayList<T> list=stack.get();
  if (list == null) {
    return null;
  }
  T rtn=list.remove(list.size() - 1);
  if (list.size() == 0) {
    stack.set(null);
    list.clear();
  }
  return rtn;
}
