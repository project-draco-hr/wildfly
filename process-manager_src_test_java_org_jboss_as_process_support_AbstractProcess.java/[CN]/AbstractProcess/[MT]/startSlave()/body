{
  clientStream=TestProcessUtils.createProcessClient(processName);
  slave=new ProcessManagerSlave(System.in,System.out,new TestHandler());
  Thread t=new Thread(this.slave.getController(),"Slave Process");
  t.start();
  started();
}
