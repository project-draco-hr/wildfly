{
  DomainConfigurationPersister service=new DomainConfigurationPersister();
  batchBuilder.addService(SERVICE_NAME,service).addDependency(DomainController.SERVICE_NAME,DomainController.class,service.injectedDomainController);
}
