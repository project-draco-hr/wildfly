{
  State nextState=update(stateValues);
  if (nextState == null && stateValues.isInteractive()) {
    nextState=new ConfirmationChoice(theConsole,MESSAGES.serverUser(),MESSAGES.yesNo(),new DisplaySecret(theConsole,stateValues),null);
  }
  return nextState;
}
