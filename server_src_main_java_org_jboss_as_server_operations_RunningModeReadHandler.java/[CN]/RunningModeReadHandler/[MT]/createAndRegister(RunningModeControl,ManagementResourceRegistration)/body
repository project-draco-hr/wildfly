{
  AttributeDefinition def=SimpleAttributeDefinitionBuilder.create(RUNNING_MODE,ModelType.STRING).setValidator(new EnumValidator(RunningMode.class,false,false)).setFlags(AttributeAccess.Flag.STORAGE_RUNTIME).build();
  resourceRegistration.registerReadOnlyAttribute(def,new RunningModeReadHandler(runningModeControl));
}
