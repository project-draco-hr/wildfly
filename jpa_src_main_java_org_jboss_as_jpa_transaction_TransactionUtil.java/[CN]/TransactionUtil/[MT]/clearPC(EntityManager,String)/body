{
  Map<String,EntityManager> map=(Map<String,EntityManager>)session.get();
  Object result=map.remove(scopedPuName);
  if (result == null) {
    throw new RuntimeException("clearPC could not remove " + scopedPuName + " from local transaction session (either it was already removed or was never added)");
  }
}
