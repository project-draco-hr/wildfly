{
  try {
    while (!shutdown.get()) {
      byte[] bytes=StreamUtils.readBytesWithLength(input);
      messageHandler.handleMessage(serverName,bytes);
    }
  }
 catch (  IOException e) {
  }
 finally {
    shutdown();
  }
}
