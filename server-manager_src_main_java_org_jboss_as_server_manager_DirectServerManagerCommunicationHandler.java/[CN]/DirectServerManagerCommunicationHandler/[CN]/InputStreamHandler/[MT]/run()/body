{
  try {
    while (!shutdown.get()) {
      byte[] bytes=StreamUtils.readBytesWithLength(input);
      messageHandler.handleCommand(serverName,bytes);
    }
  }
 catch (  IOException e) {
  }
 finally {
    shutdown();
  }
}
