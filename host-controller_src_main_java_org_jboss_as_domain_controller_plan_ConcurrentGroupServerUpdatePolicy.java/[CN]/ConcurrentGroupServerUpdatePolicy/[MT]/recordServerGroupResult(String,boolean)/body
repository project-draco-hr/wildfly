{
synchronized (this) {
    if (groups.contains(serverGroup)) {
      responseCount++;
      if (failed) {
        this.failed=true;
      }
      notifyAll();
    }
 else {
      throw MESSAGES.unknownServerGroup(serverGroup);
    }
  }
}
