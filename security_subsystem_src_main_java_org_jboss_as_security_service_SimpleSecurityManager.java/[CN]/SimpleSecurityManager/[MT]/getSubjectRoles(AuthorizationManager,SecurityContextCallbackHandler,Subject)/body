{
  if (System.getSecurityManager() == null) {
    return am.getSubjectRoles(authenticatedSubject,scb);
  }
  return AccessController.doPrivileged(new PrivilegedAction<RoleGroup>(){
    @Override public RoleGroup run(){
      return am.getSubjectRoles(authenticatedSubject,scb);
    }
  }
);
}
