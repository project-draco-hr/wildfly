{
  final DeploymentPlanResult result=null;
  try {
    output.writeByte(ManagementProtocol.PARAM_DEPLOYMENT_PLAN_RESULT);
    marshal(output,result);
  }
 catch (  Exception e) {
    throw new ManagementException("Unable to send deployment plan result.",e);
  }
}
