{
  try {
    if (executor != null) {
      executor.shutdown();
    }
  }
  finally {
    if (socket != null)     try {
      socket.close();
    }
 catch (    IOException e) {
      log.warn("Failed to close connection to domain controller",e);
    }
  }
}
