{
  writer.writeEmptyElement(Element.REPLACE.getLocalName());
  writer.writeAttribute(Attribute.PATTERN.getLocalName(),pattern);
  writer.writeAttribute(Attribute.REPLACEMENT.getLocalName(),replacement);
  writer.writeAttribute(Attribute.REPLACE_ALL.getLocalName(),Boolean.toString(replaceAll));
}
