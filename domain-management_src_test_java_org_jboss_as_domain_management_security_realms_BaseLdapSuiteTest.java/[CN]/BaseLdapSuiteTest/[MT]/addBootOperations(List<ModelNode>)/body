{
  bootOperations.add(OutboundConnectionAddBuilder.builder(CONNECTION_NAME).setUrl("ldap://" + HOST_NAME + ":"+ LDAP_PORT).setSearchDn("uid=wildfly,dc=simple,dc=wildfly,dc=org").setSearchCredential("wildfly_password").build());
  super.addBootOperations(bootOperations);
}
