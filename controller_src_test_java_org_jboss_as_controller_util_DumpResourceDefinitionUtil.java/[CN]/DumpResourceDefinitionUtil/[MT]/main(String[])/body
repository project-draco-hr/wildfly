{
  ModelNode resourceDefinition=Tools.getCurrentRunningResourceDefinition(PathAddress.EMPTY_ADDRESS);
  Tools.serializeModeNodeToFile(resourceDefinition,new File("target/" + ResourceType.STANDALONE.toString().toLowerCase() + "-resource-definition-running.dmr"));
}
