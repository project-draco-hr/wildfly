{
  try {
    byte[] bytes=new byte[1024];
    int r=input.read(bytes);
    while (r > 0) {
      output.write(bytes,0,r);
      r=input.read(bytes);
    }
  }
 catch (  IOException e) {
    throw e;
  }
 finally {
    input.close();
    output.close();
  }
}
