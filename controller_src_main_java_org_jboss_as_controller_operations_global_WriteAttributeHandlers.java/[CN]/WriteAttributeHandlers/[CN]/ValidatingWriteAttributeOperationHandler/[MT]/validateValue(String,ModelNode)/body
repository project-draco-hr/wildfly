{
  if (!value.isDefined()) {
    if (!nullable) {
      return "Attribute " + name + " may not be null ";
    }
  }
 else {
    if (value.getType() != type && (!allowExpressions || value.getType() != ModelType.EXPRESSION)) {
      return "Wrong type for " + name + ". Expected "+ type+ " but was "+ value.getType();
    }
  }
  return null;
}
