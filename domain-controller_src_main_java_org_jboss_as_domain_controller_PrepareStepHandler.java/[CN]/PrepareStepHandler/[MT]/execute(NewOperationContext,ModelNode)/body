{
  if (operation.hasDefined(OPERATION_HEADERS) && operation.get(OPERATION_HEADERS,EXECUTE_FOR_COORDINATOR).asBoolean(false)) {
    executeForCoordinator(context,operation);
  }
 else {
    executeAsCoordinator(context,operation);
  }
}
