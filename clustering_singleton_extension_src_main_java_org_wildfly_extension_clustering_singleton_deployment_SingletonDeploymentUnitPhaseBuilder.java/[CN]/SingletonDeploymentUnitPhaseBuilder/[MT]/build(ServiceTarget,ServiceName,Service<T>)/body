{
  return this.policy.getSingletonServiceBuilderFactory().createSingletonServiceBuilder(name,service).electionPolicy(this.policy.getElectionPolicy()).requireQuorum(this.policy.getQuorum()).build(target).setInitialMode(ServiceController.Mode.ACTIVE);
}
