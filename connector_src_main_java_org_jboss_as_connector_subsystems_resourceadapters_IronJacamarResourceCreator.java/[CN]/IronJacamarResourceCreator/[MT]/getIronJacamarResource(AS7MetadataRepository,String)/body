{
  final Resource resource=Resource.Factory.create();
  Activation activation=mdr.getIronJacamarMetaData(name);
  if (activation != null)   addResourceAdapter(resource,name,activation);
  return resource;
}
