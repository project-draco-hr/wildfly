{
  if (contained.getRepository() != delegateFor.getRepository())   throw IIOPMessages.MESSAGES.wrongInterfaceRepository();
  if (contMap.get(name) != null)   throw IIOPMessages.MESSAGES.duplicateRepositoryName();
  cont.add(contained);
  contMap.put(name,contained);
}
