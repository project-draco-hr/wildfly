{
  if (name == null || (name.length() > 1 && name.endsWith("/")))   return OSGiMessages.MESSAGES.invalidResourceName(name);
  if (!name.startsWith("/"))   return OSGiMessages.MESSAGES.invalidResourceName(name);
  return null;
}
