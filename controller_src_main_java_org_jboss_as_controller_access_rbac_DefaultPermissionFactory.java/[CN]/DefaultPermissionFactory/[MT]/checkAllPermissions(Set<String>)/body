{
  if (roles.contains(StandardRole.SUPERUSER.toString()) && (authorizerConfiguration.getPermissionCombinationPolicy() == CombinationPolicy.PERMISSIVE || roles.size() == 1)) {
    return AllPermissionsCollection.INSTANCE;
  }
  return null;
}
