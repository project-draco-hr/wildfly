{
  ProtocolMetaData protocol=new ProtocolMetaData().addContext(new HTTPContext(containerConfig.getBindAddress().getHostAddress(),containerConfig.getHttpPort()).add(new Servlet("ArquillianServletRunner",getContextRootName(archive))));
  return protocol;
}
