{
  DeploymentChainProvider.INSTANCE.addDeploymentChain(new DeploymentChainImpl("test.chain"),new DeploymentChainProvider.Selector(){
    public boolean supports(    VirtualFile root){
      return "testDeployment".equals(root.getName());
    }
  }
);
}
