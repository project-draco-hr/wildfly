{
  if (serverSocket != null) {
    try {
      serverSocket.close();
      serverSocket=null;
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
  if (executorService != null) {
    executorService.shutdown();
  }
}
