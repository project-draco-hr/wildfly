{
  ModelNode ret=modelControllerClient.execute(op);
  if (!unwrapResult)   return ret;
  assertTrue("Management operation " + op.asString() + " failed: "+ ret.asString(),SUCCESS.equals(ret.get(OUTCOME).asString()));
  return ret.get(RESULT);
}
