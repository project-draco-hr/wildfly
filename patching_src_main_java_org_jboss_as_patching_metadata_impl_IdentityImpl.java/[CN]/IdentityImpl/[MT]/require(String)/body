{
  if (patchId == null) {
    throw new IllegalArgumentException("patchId is null");
  }
  if (requires.isEmpty()) {
    requires=new ArrayList<String>();
  }
  requires.add(patchId);
  return this;
}
