{
  int hostPos=pos;
  if ((pos=parseHost(chars,pos)) == -1) {
    return -1;
  }
  if (Chars.isCharASCII(chars,pos,':')) {
    if (pos == hostPos) {
      return -1;
    }
    pos++;
  }
 else {
    return pos;
  }
  if ((pos=parsePort(chars,pos)) == -1) {
    return -1;
  }
  return pos;
}
