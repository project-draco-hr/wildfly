{
  Configuration config=new Configuration(state);
  batchBuilder.addService(SERVICE_NAME,config).addDependency(ServerEnvironmentService.SERVICE_NAME,ServerEnvironment.class,config.injectedEnvironment).setInitialMode(Mode.ACTIVE).install();
}
