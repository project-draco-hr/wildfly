{
  super(javaName);
  if (type == Void.TYPE || !type.isPrimitive() && type != java.lang.String.class)   throw ORBMessages.MESSAGES.badConstantType(type.getName());
  this.type=type;
  this.value=value;
}
