{
  return new BeanContainerConfigImpl(name != null ? name : beanClass.getName(),beanClass,beanClassLoader,resourceInjections.toArray(new ResourceInjectionConfiguration[resourceInjections.size()]),postConstructMethods.toArray(new Method[postConstructMethods.size()]),preDestroyMethods.toArray(new Method[preDestroyMethods.size()]),interceptorConfigurations.toArray(new MethodInterceptorConfiguration[interceptorConfigurations.size()]),containerFactory);
}
