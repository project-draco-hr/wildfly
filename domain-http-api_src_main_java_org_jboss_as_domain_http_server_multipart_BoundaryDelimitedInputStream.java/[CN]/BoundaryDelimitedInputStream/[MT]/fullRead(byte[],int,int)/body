{
  int count=0;
  int read=0;
  do {
    read=source.read(buffer,offset + count,length - count);
    if (read > 0)     count+=read;
  }
 while (read > 0 && count < length);
  if (read < 0)   realEof=true;
  return count;
}
