{
  String args=ctx.getCommandArguments();
  if (args != null) {
    args=args.trim();
    if (args.isEmpty()) {
      args=null;
    }
 else     if (args.equals("--help")) {
      printHelp(ctx);
      return;
    }
  }
  handle(ctx,args);
}
