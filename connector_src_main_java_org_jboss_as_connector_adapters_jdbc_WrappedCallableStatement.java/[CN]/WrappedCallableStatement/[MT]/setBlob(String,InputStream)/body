{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setBlob(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,inputStream);
    statement.setBlob(parameterName,inputStream);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
