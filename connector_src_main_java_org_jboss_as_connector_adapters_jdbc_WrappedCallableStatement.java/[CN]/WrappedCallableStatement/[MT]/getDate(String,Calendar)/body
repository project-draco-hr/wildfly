{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getDate(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,calendar);
    return cs.getDate(parameterName,calendar);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
