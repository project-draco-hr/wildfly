{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setBinaryStream(%s, %s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,x,length);
    statement.setBinaryStream(parameterName,x,length);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
