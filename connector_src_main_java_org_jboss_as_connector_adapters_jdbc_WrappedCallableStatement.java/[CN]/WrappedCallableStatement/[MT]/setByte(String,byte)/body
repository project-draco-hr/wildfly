{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setByte(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setByte(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
