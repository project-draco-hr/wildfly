{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getFloat(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName);
    return cs.getFloat(parameterName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
