{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getCharacterStream(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterIndex);
    return statement.getCharacterStream(parameterIndex);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
