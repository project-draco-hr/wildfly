{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getTimestamp(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName);
    return cs.getTimestamp(parameterName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
