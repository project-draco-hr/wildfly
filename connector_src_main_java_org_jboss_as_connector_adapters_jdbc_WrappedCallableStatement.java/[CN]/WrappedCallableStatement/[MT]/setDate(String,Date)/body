{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setDate(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setDate(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
