{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setURL(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setURL(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
