{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getDate(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterIndex);
    return cs.getDate(parameterIndex);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
