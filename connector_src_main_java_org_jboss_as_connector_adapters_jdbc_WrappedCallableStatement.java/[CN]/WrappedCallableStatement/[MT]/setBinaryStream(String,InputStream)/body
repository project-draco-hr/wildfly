{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setBinaryStream(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,x);
    statement.setBinaryStream(parameterName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
