{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setInt(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setInt(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
