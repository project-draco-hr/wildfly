{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getClob(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterIndex);
    return cs.getClob(parameterIndex);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
