{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setCharacterStream(%s, %s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,reader,length);
    cs.setCharacterStream(parameterName,reader,length);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
