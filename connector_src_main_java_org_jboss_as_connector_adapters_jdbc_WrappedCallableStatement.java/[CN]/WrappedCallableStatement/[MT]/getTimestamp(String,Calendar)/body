{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getTimestamp(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,calendar);
    return cs.getTimestamp(parameterName,calendar);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
