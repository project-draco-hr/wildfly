{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setNString(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    statement.setNString(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
