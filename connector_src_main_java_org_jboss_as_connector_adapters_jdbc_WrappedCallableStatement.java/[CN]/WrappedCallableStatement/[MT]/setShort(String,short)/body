{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setShort(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setShort(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
