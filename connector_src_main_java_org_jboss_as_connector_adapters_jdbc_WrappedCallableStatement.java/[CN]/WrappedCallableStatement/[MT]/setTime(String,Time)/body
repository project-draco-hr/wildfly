{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setTime(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setTime(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
