{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setTime(%s, %s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value,calendar);
    cs.setTime(parameterName,value,calendar);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
