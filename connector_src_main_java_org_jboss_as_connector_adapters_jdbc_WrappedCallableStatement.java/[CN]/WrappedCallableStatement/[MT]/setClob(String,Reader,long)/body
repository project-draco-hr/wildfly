{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setClob(%s, %s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,reader,length);
    statement.setClob(parameterName,reader,length);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
