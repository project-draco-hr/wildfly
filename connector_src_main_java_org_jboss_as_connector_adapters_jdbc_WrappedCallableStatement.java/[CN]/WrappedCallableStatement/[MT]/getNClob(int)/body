{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getNClob(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterIndex);
    return statement.getNClob(parameterIndex);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
