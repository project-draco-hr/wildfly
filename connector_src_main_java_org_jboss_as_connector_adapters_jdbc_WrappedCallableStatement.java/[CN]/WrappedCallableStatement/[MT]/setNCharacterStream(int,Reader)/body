{
  PreparedStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setNCharacterStream(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterIndex,value);
    statement.setNCharacterStream(parameterIndex,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
