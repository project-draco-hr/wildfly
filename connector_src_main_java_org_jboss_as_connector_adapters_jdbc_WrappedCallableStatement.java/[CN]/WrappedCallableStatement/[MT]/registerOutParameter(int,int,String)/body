{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] registerOutParameter(%s, %s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterIndex,sqlType,typeName);
    cs.registerOutParameter(parameterIndex,sqlType,typeName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
