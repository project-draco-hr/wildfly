{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getInt(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName);
    return cs.getInt(parameterName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
