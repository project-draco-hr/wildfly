{
  PreparedStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setCharacterStream(%s, %s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterIndex,reader,length);
    statement.setCharacterStream(parameterIndex,reader,length);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
