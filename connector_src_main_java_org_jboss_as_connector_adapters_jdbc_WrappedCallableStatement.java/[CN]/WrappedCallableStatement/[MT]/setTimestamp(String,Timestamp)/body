{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setTimestamp(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setTimestamp(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
