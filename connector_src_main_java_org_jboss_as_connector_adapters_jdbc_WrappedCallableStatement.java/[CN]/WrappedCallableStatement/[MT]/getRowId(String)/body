{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getRowId(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName);
    return statement.getRowId(parameterName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
