{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getDate(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName);
    return cs.getDate(parameterName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
