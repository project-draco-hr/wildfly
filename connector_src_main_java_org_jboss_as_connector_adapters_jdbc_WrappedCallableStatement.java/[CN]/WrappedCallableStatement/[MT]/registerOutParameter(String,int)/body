{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] registerOutParameter(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,sqlType);
    cs.registerOutParameter(parameterName,sqlType);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
