{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setBoolean(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setBoolean(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
