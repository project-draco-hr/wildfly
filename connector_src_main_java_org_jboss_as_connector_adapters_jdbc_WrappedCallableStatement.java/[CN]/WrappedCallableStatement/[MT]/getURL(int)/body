{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getURL(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterIndex);
    return cs.getURL(parameterIndex);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
