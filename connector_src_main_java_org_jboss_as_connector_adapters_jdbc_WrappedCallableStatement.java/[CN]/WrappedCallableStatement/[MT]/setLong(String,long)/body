{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setLong(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setLong(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
