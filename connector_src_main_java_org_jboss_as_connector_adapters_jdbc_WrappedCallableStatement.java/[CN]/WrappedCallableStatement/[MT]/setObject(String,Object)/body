{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setObject(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setObject(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
