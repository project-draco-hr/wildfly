{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getNCharacterStream(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName);
    return statement.getCharacterStream(parameterName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
