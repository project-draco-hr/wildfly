{
  CallableStatement statement=getUnderlyingStatement();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setRowId(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,x);
    statement.setRowId(parameterName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
