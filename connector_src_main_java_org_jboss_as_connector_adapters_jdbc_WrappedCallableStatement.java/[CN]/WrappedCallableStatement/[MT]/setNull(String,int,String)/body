{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setNull(%s, %s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,sqlType,typeName);
    cs.setNull(parameterName,sqlType,typeName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
