{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] registerOutParameter(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterIndex,sqlType);
    cs.registerOutParameter(parameterIndex,sqlType);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
