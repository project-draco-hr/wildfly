{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] setString(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_CALLABLE_STATEMENT,parameterName,value);
    cs.setString(parameterName,value);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
