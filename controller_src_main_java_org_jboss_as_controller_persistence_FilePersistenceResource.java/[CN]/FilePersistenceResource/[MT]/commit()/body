{
  if (marshalled == null) {
    throw MESSAGES.rollbackAlreadyInvoked();
  }
  try {
    writeToTempFile();
    moveTempFileToMain();
  }
 catch (  Exception e) {
    MGMT_OP_LOGGER.failedToStoreConfiguration(e,fileName.getName());
  }
}
