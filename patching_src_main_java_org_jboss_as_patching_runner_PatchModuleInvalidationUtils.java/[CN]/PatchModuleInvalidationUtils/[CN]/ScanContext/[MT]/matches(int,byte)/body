{
  if (pos == 0) {
    if (b == good) {
      state=State.FOUND;
      return true;
    }
 else     if (b == bad) {
      state=State.NOTHING_TODO;
      return true;
    }
    return false;
  }
 else {
    return pattern[pos] == b;
  }
}
