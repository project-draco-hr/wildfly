{
synchronized (this) {
    closed=true;
    if (endpoint != null) {
      endpoint.close();
      endpoint=null;
    }
    if (strategy != null) {
      strategy.close();
      strategy=null;
    }
    super.shutdownNow();
  }
}
