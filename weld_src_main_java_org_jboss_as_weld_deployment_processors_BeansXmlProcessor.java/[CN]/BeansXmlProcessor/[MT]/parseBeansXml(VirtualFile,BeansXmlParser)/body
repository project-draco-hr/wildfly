{
  try {
    return parser.parse(beansXmlFile.asFileURL());
  }
 catch (  MalformedURLException e) {
    throw new DeploymentUnitProcessingException("Could get beans.xml file as URL when processing file: " + beansXmlFile,e);
  }
}
