{
  int jdbcType=jdbcTypes[0];
  return jdbcType != Types.BINARY && jdbcType != Types.BLOB && jdbcType != Types.CLOB && jdbcType != Types.LONGVARBINARY && jdbcType != Types.LONGVARCHAR && jdbcType != Types.VARBINARY;
}
