{
  try {
    ServerManagerConnectionProtocol.OutgoingCommand.UPDATE_DOMAIN.execute(this,socketOut);
  }
 catch (  Exception e) {
    throw new RuntimeException("Failed to update server manager with new domain",e);
  }
}
