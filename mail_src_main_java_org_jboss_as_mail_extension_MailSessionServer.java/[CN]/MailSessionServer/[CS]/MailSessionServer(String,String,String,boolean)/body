{
  this.outgoingSocketBinding=outgoingSocketBinding;
  this.sslEnabled=ssl;
  if (username != null) {
    this.credentials=new Credentials(username,password);
  }
 else {
    credentials=null;
  }
}
