{
  if (obj == null)   throw new IllegalArgumentException("Null DeployableObject");
  if (obj.getType().equals(ModuleType.WAR))   return new WarConfiguration(obj);
  throw new InvalidModuleException("CreateConfiguration: Module type not supported: " + obj.getType());
}
