{
  return new LdapUserSearcherService(new LdapUserSearcher(){
    @Override public LdapEntry userSearch(    DirContext dirContext,    String suppliedName){
      return new LdapEntry(suppliedName,suppliedName);
    }
  }
);
}
