{
  if (_nc == null)   throw ORBMessages.MESSAGES.notANamingContext(nc.toString());
  try {
    nc.destroy();
  }
 catch (  Exception e) {
    throw org.jboss.as.jdkorb.naming.jndi.ExceptionMapper.mapException(e,this,null);
  }
}
