{
  if (_nc == null)   throw ORBMessages.MESSAGES.notANamingContext(oldName.toString());
  if (oldName.size() == 0 || newName.size() == 0)   throw ORBMessages.MESSAGES.invalidEmptyName();
  java.lang.Object obj=lookup(oldName);
  bind(newName,obj);
  unbind(oldName);
}
