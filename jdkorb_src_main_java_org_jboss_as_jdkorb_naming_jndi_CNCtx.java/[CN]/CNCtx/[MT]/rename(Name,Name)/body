{
  if (_nc == null)   throw JdkORBMessages.MESSAGES.notANamingContext(oldName.toString());
  if (oldName.size() == 0 || newName.size() == 0)   throw JdkORBMessages.MESSAGES.invalidEmptyName();
  java.lang.Object obj=lookup(oldName);
  bind(newName,obj);
  unbind(oldName);
}
