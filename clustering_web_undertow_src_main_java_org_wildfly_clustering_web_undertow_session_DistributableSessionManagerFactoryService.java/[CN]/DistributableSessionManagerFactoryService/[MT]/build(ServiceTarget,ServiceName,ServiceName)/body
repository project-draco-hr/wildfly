{
  DistributableSessionManagerFactoryService service=new DistributableSessionManagerFactoryService();
  return target.addService(name,service).addDependency(factoryServiceName,org.wildfly.clustering.web.session.SessionManagerFactory.class,service.factory);
}
