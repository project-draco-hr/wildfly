{
  if (modelNode == null) {
    return null;
  }
  try {
    return remote.deserializeModelNode(local.serializeModelNode(modelNode));
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
