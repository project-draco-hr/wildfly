{
  try {
    Bean<UUID,K,V> bean=this.manager.findBean(value.getId());
    if (bean != null) {
      if (bean.release()) {
        bean.close();
      }
    }
  }
  finally {
    value.getCacheContext().close();
  }
}
