{
  K id=value.getId();
  try {
    Bean<UUID,K,V> bean=this.manager.findBean(id);
    if (bean != null) {
      if (bean.release()) {
        bean.close();
      }
    }
  }
  finally {
    BatchStack.popBatch().close();
  }
}
