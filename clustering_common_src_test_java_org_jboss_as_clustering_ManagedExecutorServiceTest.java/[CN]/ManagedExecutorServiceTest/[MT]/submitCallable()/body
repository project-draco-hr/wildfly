{
  @SuppressWarnings("unchecked") Future<Object> expected=mock(Future.class);
  Task task=new Task();
  when(this.executor.submit(task)).thenReturn(expected);
  Future<Object> result=this.subject.submit(task);
  assertSame(expected,result);
}
