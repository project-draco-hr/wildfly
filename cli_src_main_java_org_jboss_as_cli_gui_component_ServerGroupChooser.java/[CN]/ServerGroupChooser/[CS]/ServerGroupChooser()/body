{
  setLayout(new BorderLayout());
  setBorder(new TitledBorder("Server Groups"));
  setServerGroups();
  add(serverGroupsPanel,BorderLayout.CENTER);
  if (!isStandalone())   serverGroups.get(0).setSelected(true);
}
