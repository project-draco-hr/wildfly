{
  @SuppressWarnings("unchecked") Value<Channel> channel=(Value<Channel>)properties.get(CHANNEL);
  if (channel == null) {
    throw MESSAGES.invalidTransportProperty(CHANNEL,properties);
  }
  return channel.getValue();
}
