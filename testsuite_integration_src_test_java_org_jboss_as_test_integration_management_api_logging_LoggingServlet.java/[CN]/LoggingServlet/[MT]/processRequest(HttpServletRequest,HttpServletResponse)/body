{
  response.setContentType("text/plain");
  PrintWriter out=response.getWriter();
  String msg=request.getParameter("msg");
  if (msg == null)   msg="Logging servlet.";
  out.println(msg);
  out.close();
  for (  Logger.Level level : Logger.Level.values())   log.log(level,msg);
}
