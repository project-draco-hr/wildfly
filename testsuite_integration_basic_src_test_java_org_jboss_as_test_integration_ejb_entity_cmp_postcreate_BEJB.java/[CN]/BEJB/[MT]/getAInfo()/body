{
  try {
    if (isA()) {
      ALocal a=getA();
      ADVC aDVC=new ADVC(a.getId(),a.getName(),a.getValue());
      return aDVC;
    }
 else     return null;
  }
 catch (  Exception e) {
    throw new EJBException("Exception occurred: " + e);
  }
}
