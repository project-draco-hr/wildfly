{
  if (closeAtTxCompletion) {
    if (log.isDebugEnabled())     log.debug(getEntityManagerDetails(manager) + ": closing entity managersession ");
    manager.close();
  }
  getInstance().putEntityManagerInTransactionRegistry(scopedPuName,null);
}
