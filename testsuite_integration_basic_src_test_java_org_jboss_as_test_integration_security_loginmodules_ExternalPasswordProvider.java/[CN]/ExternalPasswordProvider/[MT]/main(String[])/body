{
  String password=null;
  if (args != null && args.length == 1) {
    increaseFileCounter();
    password=args[0];
  }
 else {
    password="secret";
  }
  System.out.println(password);
  System.out.flush();
}
