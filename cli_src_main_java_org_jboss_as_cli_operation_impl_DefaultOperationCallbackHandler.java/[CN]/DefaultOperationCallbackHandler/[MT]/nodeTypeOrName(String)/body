{
  if (address == null) {
    address=new DefaultOperationRequestAddress();
  }
  if (address.endsOnType()) {
    nodeName(typeOrName);
  }
 else {
    nodeType(typeOrName);
  }
  separator=SEPARATOR_NONE;
}
