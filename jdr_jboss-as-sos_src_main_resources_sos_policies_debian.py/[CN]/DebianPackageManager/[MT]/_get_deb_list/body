def _get_deb_list(self):
    pkg_list = shell_out(['dpkg-query', '-W', '-f=', "'${Package}|${Version}\\n' \\*"]).splitlines()
    self._debs = {}
    for pkg in pkg_list:
        (name, version) = pkg.split('|')
        self._debs[name] = {'name': name, 'version': version, }
