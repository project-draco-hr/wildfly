{
  final JMSService service=new JMSService();
  return target.addService(JMSServices.JMS_MANAGER,service).addDependency(MessagingServices.JBOSS_MESSAGING,HornetQServer.class,service.getHornetQServer()).addListener(listeners).setInitialMode(Mode.ACTIVE).install();
}
