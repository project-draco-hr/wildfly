{
  final JMSService service=new JMSService();
  builder.addService(JMSSubsystemElement.JMS_MANAGER,service).addDependency(MessagingSubsystemElement.JBOSS_MESSAGING,HornetQServer.class,service.getHornetQServer()).setInitialMode(Mode.IMMEDIATE);
}
