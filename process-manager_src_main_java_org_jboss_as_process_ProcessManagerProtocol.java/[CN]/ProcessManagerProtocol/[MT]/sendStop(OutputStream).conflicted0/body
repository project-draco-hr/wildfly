{
synchronized (output) {
    StreamUtils.writeString(output,OutgoingPmCommand.SHUTDOWN + "\n");
    output.flush();
    try {
      output.close();
    }
 catch (    IOException ignore) {
    }
  }
}
