{
  try {
    readyLatch.countDown();
    startLatch.await(10,TimeUnit.SECONDS);
    result=ts.testee.lock("test",1000,newLock);
  }
 catch (  Exception e) {
    this.exception=e;
  }
 finally {
    endLatch.countDown();
  }
}
