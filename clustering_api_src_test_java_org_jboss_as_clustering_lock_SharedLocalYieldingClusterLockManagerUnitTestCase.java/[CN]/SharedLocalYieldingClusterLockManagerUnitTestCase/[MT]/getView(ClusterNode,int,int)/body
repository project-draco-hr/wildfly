{
  List<ClusterNode> all=new ArrayList<ClusterNode>(Arrays.asList(node1,node2,node3));
  all.remove(member);
  while (all.size() > numMembers - 1) {
    all.remove(all.size() - 1);
  }
  all.add(viewPos,member);
  return all;
}
