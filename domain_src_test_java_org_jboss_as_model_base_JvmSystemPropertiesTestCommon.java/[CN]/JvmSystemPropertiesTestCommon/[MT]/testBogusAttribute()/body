{
  String fullcontent=getter.getFullContent("<system-properties><property name=\"prop1\" value=\"value1\" bogus=\"bad\"/></system-properties>");
  try {
    ModelParsingSupport.parseDomainModel(getter.getXMLMapper(),fullcontent);
    fail("Bogus attribute did not cause parsing failure");
  }
 catch (  XMLStreamException good) {
  }
}
