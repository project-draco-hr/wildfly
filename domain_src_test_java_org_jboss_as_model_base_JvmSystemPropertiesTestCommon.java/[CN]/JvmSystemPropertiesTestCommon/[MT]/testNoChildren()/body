{
  String fullcontent=getter.getFullContent("<system-properties/>");
  try {
    ModelParsingSupport.parseDomainModel(getter.getXMLMapper(),fullcontent);
    fail("Missing child element did not cause parsing failure");
  }
 catch (  XMLStreamException good) {
  }
}
