{
  String fullcontent=getter.getFullContent("<system-properties><property value=\"value1\"/></system-properties>");
  try {
    ModelParsingSupport.parseDomainModel(getter.getXMLMapper(),fullcontent);
    fail("Missing name attribute did not cause parsing failure");
  }
 catch (  XMLStreamException good) {
  }
}
