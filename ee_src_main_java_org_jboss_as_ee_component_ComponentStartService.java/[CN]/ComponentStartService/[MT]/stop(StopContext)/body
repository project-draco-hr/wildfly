{
  Runnable r=new Runnable(){
    @Override public void run(){
synchronized (this) {
        try {
          getValue().stop();
        }
  finally {
          context.complete();
        }
      }
    }
  }
;
synchronized (r) {
    executor.getValue().submit(r);
    context.asynchronous();
  }
}
