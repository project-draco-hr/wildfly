{
  context.asynchronous();
  executor.getValue().submit(new Runnable(){
    @Override public void run(){
      try {
        getValue().stop();
      }
  finally {
        context.complete();
      }
    }
  }
);
}
