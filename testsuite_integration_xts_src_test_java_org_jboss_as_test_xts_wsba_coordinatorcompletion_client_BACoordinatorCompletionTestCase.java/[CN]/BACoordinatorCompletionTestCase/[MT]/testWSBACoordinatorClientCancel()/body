{
  uba.begin();
  client1.saveData();
  client2.saveData();
  client3.saveData();
  uba.cancel();
  assertEventLogClient1(CANCEL);
  assertEventLogClient2(CANCEL);
  assertEventLogClient3(CANCEL);
}
