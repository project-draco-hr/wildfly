{
  if (bootFile == null) {
synchronized (this) {
      if (bootFile == null) {
        if (bootFileName.equals(rawFileName)) {
          bootFile=mainFile;
        }
 else {
          bootFile=determineBootFile(configurationDir,bootFileName);
        }
      }
    }
  }
  return bootFile;
}
