{
  if (confirmed) {
    log.info("[SERVICE] Participant.confirmCompleted('" + confirmed + "') (This tells the participant that compensation information has been logged and that it is safe to commit any changes.)");
    MockSetManager.commit();
  }
 else {
    MockSetManager.rollback(value);
  }
}
