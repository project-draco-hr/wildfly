{
  long end=System.currentTimeMillis() + timeoutMillis;
  do {
    if (!managerAlive(addr,port))     return;
    Thread.sleep(300);
  }
 while (System.currentTimeMillis() < end);
  Assert.fail("ServerManager did not stop in " + timeoutMillis + "ms");
}
