{
  final WeldClassIntrospector introspector=new WeldClassIntrospector();
  serviceTarget.addService(serviceName(deploymentUnit),new ValueService<Object>(new ImmediateValue<Object>(introspector))).addDependency(BeanManagerService.serviceName(deploymentUnit),BeanManager.class,introspector.beanManager).install();
}
