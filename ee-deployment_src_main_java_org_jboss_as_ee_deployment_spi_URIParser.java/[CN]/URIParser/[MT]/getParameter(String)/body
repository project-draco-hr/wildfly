{
  String value=null;
  String query=uri.getQuery();
  String[] params={};
  if (query != null) {
    params=query.split("&|=");
  }
  for (int n=0; n < params.length; n+=2) {
    if (key.equals(params[n])) {
      value=params[n + 1];
    }
  }
  return value;
}
