{
  super();
  this.processor=processor;
  if (name == null)   name="AsynchEventHandler";
  this.name=name;
  this.log=Logger.getMessageLogger(ClusteringImplLogger.class,processor.getClass().getName() + "." + name);
}
