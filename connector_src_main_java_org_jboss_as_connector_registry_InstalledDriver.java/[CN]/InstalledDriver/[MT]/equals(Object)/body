{
  if (obj == this)   return true;
  if (obj instanceof InstalledDriver) {
    InstalledDriver other=(InstalledDriver)obj;
    return driverClassName.equals(other.driverClassName) && majorVersion == other.majorVersion && minorVersion == other.minorVersion;
  }
  return false;
}
