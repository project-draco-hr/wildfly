{
  final WebArchive war=ShrinkWrap.create(WebArchive.class,"sessionsynchronization.war").addPackage(SynchedStatefulBean.class.getPackage()).addPackage(SynchedStatefulBean2.class.getPackage()).addAsWebInfResource("sessionsynchronization/ejb-jar.xml","ejb-jar.xml");
  log.info(war.toString(true));
  return war;
}
