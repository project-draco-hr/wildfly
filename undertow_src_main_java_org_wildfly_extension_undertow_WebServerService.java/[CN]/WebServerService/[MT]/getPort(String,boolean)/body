{
  if (secure) {
    if (httpsPort == -1) {
      throw UndertowMessages.MESSAGES.noPortListeningForProtocol(protocol);
    }
    return httpsPort;
  }
  if (httpPort == -1) {
    throw UndertowMessages.MESSAGES.noPortListeningForProtocol(protocol);
  }
  return httpPort;
}
