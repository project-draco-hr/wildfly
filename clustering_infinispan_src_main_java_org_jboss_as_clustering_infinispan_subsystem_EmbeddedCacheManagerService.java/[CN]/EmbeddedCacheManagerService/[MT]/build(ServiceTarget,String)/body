{
  EmbeddedCacheManagerService service=new EmbeddedCacheManagerService();
  return target.addService(getServiceName(name),service).addDependency(EmbeddedCacheManagerConfigurationService.getServiceName(name),EmbeddedCacheManagerConfiguration.class,service.config);
}
