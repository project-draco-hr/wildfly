{
  if (o == null)   throw new ClassCastException("Comparison to null value");
  if (!(o instanceof ClusterNodeImpl))   throw new ClassCastException("Comparison between different classes");
  return this.id.compareTo(o.getName());
}
