{
  DistributableSessionIdentifierCodecService service=new DistributableSessionIdentifierCodecService();
  ServiceBuilder<SessionIdentifierCodec> builder=target.addService(name,service).addDependency(locatorServiceName,RouteLocator.class,service.locator);
  return builder;
}
