{
  WebArchive archive=ShrinkWrap.create(WebArchive.class,"logging.war");
  archive.addClasses(LoggingServlet.class);
  archive.addAsManifestResource(new StringAsset("Logging-Profile: non-existing-profile \n"),"MANIFEST.MF");
  return archive;
}
