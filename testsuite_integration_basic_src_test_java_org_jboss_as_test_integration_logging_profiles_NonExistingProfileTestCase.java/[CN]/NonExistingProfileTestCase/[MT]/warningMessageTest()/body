{
  BufferedReader br=new BufferedReader(new InputStreamReader(new FileInputStream(loggingTestLog),Charset.forName("UTF-8")));
  String line;
  boolean warningFound=false;
  while ((line=br.readLine()) != null) {
    if (line.contains("JBAS011509")) {
      warningFound=true;
      break;
    }
  }
  br.close();
  Assert.assertTrue(warningFound);
}
