{
  BufferedReader br=new BufferedReader(new InputStreamReader(new FileInputStream(loggingTestLog),StandardCharsets.UTF_8));
  String line;
  boolean warningFound=false;
  while ((line=br.readLine()) != null) {
    if (line.contains("WFLYLOG0010")) {
      warningFound=true;
      break;
    }
  }
  br.close();
  Assert.assertTrue(warningFound);
}
