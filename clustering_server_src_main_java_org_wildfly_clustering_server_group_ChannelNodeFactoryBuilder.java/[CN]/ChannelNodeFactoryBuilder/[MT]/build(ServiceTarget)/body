{
  return target.addService(this.getServiceName(),this).addDependency(JGroupsRequirement.CHANNEL.getServiceName(this.support,this.group),Channel.class,this.channel).setInitialMode(ServiceController.Mode.PASSIVE);
}
