{
  ConnectedChannelService service=new ConnectedChannelService(cluster);
  return target.addService(getServiceName(channel),service).addDependency(ChannelService.getServiceName(channel),Channel.class,service.channel);
}
