{
  final JavaArchive jar=ShrinkWrap.create(JavaArchive.class,"test-containerstart.jar").addClass(SendMessagesTestCase.class).addClass(Base64.class).addClass(ShrinkWrapUtils.class).addClass(PollingUtils.class).addAsManifestResource(new StringAsset("Dependencies: org.jboss.as.controller-client, org.jboss.dmr, deployment." + SINGLETON + ".jar \n"),"MANIFEST.MF");
  log.info(jar.toString(true));
  return jar;
}
