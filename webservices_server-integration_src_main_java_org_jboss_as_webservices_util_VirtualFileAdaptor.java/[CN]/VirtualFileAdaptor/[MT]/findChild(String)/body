{
  final VirtualFile virtualFile=getFile();
  final VirtualFile childFile=file.getChild(child);
  if (!childFile.exists())   throw MESSAGES.missingChild(child,virtualFile);
  return new VirtualFileAdaptor(childFile);
}
