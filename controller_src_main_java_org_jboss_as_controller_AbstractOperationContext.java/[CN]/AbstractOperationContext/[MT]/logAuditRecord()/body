{
  if (!auditLogged) {
    try {
      Subject subject=SubjectUtils.getCurrent();
      auditLogger.log(!isModelAffected(),resultAction,getCallerUserId(subject),getDomainUUID(),getAccessMechanism(),getSubjectInetAddress(subject),getModel(),controllerOperations);
      auditLogged=true;
    }
 catch (    Exception e) {
      ControllerLogger.MGMT_OP_LOGGER.failedToUpdateAuditLog(e);
    }
  }
}
