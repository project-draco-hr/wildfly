{
  AsyncBeanRemoteInterface bean=(AsyncBeanRemoteInterface)getInitialContext(baseUrl.getHost()).lookup(ARCHIVE_NAME + "/" + AsyncBeanRemote.class.getSimpleName()+ "!"+ AsyncBeanRemoteInterface.class.getName());
  bean.asyncMethod();
}
