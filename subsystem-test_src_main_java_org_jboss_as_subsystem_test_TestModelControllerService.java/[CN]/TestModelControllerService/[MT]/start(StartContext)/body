{
  try {
    super.start(context);
  }
 catch (  StartException e) {
    error=e;
    latch.countDown();
    throw e;
  }
catch (  Exception e) {
    error=e;
    latch.countDown();
    throw new StartException(e);
  }
}
