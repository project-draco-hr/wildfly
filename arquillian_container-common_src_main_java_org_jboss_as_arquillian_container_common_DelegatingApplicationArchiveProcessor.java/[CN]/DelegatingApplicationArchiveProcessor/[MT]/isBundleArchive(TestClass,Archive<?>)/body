{
  if (OSGiTestEnricher.isInjectionTarget(testClass.getJavaClass()))   return true;
  Manifest manifest=ManifestUtils.getOrCreateManifest(appArchive);
  if (BundleInfo.isValidateBundleManifest(manifest))   return true;
  return false;
}
