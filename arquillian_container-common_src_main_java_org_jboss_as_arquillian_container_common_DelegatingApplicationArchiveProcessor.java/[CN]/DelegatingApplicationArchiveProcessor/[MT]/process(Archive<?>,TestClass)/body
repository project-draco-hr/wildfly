{
  ApplicationArchiveProcessor archiveProcessor=null;
  if (isBundleArchive(testClass,appArchive))   archiveProcessor=new OSGiApplicationArchiveProcessor();
 else   archiveProcessor=new ModuleApplicationArchiveProcessor();
  archiveProcessor.process(appArchive,testClass);
}
