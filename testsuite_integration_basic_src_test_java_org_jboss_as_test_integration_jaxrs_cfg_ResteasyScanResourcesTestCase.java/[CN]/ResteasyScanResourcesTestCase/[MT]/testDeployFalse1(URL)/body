{
  try {
    @SuppressWarnings("unused") String result=performCall("myjaxrs/helloworld",url);
    Assert.fail("Scan of Resources is disabled so we should not pass to there - HTTP 404 must occur!");
  }
 catch (  Exception e) {
  }
}
