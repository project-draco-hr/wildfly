{
  final Set<String> relativePath=getPath(root,file);
  final String name=file.getName();
  return new ContentItem(){
    @Override public String getName(){
      return name;
    }
    @Override public Set<String> getPath(){
      return relativePath;
    }
  }
;
}
