{
  super(path,MailExtension.getResourceDescriptionResolver(MailSubsystemModel.MAIL_SESSION,MailSubsystemModel.SERVER_TYPE),new MailServerAdd(attributes),new MailServerRemove());
  this.attributes=Arrays.asList(attributes);
}
