{
  boolean intr=false;
  try {
synchronized (this) {
      while (busyServiceCount.get() > count) {
        try {
          wait();
        }
 catch (        InterruptedException e) {
          intr=true;
        }
      }
    }
  }
  finally {
    if (intr) {
      Thread.currentThread().interrupt();
    }
  }
}
