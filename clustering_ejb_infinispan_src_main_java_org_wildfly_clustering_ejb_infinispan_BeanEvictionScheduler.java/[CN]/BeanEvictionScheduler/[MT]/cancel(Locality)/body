{
synchronized (this.evictionQueue) {
    Iterator<I> beans=this.evictionQueue.iterator();
    while (beans.hasNext()) {
      I id=beans.next();
      if (!locality.isLocal(id)) {
        beans.remove();
      }
    }
  }
}
