{
  StandardContext webContext=new StandardContext();
synchronized (contexts) {
    contexts.add(webContext);
    if (bundleContext != null)     webContext.getServletContext().setAttribute(OSGI_BUNDLECONTEXT,bundleContext);
  }
  return webContext;
}
