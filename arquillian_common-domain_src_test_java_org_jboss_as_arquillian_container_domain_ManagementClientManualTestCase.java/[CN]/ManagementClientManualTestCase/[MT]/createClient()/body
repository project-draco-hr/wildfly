{
  InetAddress address=InetAddress.getLocalHost();
  int port=9999;
  DomainClient domainClient=DomainClient.Factory.create(address,port,Authentication.getCallbackHandler());
  client=new ManagementClient(domainClient,address.getHostAddress(),port);
  deployer=new ArchiveDeployer(domainClient.getDeploymentManager());
}
