{
  long end=System.currentTimeMillis() + timeoutMs;
  while (System.currentTimeMillis() < end) {
    if (serverHandler.isClosed())     break;
    Thread.sleep(100);
  }
  Assert.assertTrue(serverHandler.isClosed());
}
