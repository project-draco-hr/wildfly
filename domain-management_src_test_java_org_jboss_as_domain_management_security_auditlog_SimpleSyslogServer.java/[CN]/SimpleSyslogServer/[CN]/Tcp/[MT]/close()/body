{
  closed.set(true);
  if (socket != null) {
    try {
      socket.shutdownInput();
    }
 catch (    Throwable e) {
    }
    try {
      socket.shutdownOutput();
    }
 catch (    Throwable e) {
    }
  }
  IoUtils.safeClose(serverSocket);
  IoUtils.safeClose(socket);
}
