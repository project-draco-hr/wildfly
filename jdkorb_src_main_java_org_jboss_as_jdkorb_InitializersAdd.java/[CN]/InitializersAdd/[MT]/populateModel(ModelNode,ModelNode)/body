{
  for (  AttributeDefinition def : InitializersDefinition.INSTANCE.getAttributes()) {
    def.validateAndSet(operation,model);
  }
  if (model.hasDefined(InitializersDefinition.SECURITY.getName()) && "on".equals(model.get(InitializersDefinition.SECURITY.getName()).asString())) {
    model.get(InitializersDefinition.SECURITY.getName()).set(JdkORBSubsystemConstants.IDENTITY);
  }
}
