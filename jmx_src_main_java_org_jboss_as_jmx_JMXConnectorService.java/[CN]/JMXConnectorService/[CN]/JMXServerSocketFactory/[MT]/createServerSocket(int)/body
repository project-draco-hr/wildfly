{
  if (port != socketBinding.getPort()) {
    throw new IllegalStateException(String.format("Received request for server socket %s on port [%d] but am configured for port [%d]",socketBinding.getName(),port,socketBinding.getPort()));
  }
  return socketBinding.createServerSocket(BACKLOG);
}
