{
  final HornetQStartupPoolService service=new HornetQStartupPoolService();
  return target.addService(MessagingServices.getHornetQStartupPoolServiceName(hqServiceName),service).addListener(listeners).setInitialMode(ServiceController.Mode.ACTIVE).install();
}
