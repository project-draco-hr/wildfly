{
  if (value.getType() == ModelType.EXPRESSION || value.getType() == ModelType.STRING) {
    String valueString=value.asString();
    return ExpressionResolver.EXPRESSION_PATTERN.matcher(valueString).matches() && VAULT_PATTERN.matcher(valueString).matches();
  }
  return false;
}
