{
  File f=new File(".").getAbsoluteFile();
  f=new File(f,"src");
  Assert.assertTrue(f.exists());
  f=new File(f,"test");
  Assert.assertTrue(f.exists());
  f=new File(f,"resources");
  Assert.assertTrue(f.exists());
  f=new File(f,name);
  Assert.assertTrue(f.exists());
  return f;
}
