{
  File f=new File(".").getAbsoluteFile();
  f=new File(f,"src");
  assertTrue(f.exists());
  f=new File(f,"test");
  assertTrue(f.exists());
  f=new File(f,"resources");
  assertTrue(f.exists());
  f=new File(f,name);
  assertTrue(f.exists());
  return f;
}
