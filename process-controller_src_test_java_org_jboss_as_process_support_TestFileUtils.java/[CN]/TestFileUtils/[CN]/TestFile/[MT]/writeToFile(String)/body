{
  FileWriter writer=null;
  try {
    writer=new FileWriter(file,true);
    writer.append(s);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
 finally {
    close(writer);
  }
}
