def test_empty_strings(self):
    self.assertEqual(json.loads('""'), '')
    self.assertEqual(json.loads(u'""'), u'')
    self.assertEqual(json.loads('[""]'), [''])
    self.assertEqual(json.loads(u'[""]'), [u''])
