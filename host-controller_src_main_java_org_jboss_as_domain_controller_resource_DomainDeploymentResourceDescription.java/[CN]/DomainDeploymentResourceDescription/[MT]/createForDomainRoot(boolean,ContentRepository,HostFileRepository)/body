{
  return new DomainDeploymentResourceDescription(DeploymentResourceParent.DOMAIN,isMaster ? new DeploymentAddHandler(contentRepository) : new DeploymentAddHandler(),isMaster ? DeploymentRemoveHandler.createForMaster(contentRepository) : DeploymentRemoveHandler.createForSlave(fileRepository));
}
