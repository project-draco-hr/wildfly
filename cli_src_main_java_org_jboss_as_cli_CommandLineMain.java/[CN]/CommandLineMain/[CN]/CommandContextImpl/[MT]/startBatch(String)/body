{
  if (batchMode) {
    return false;
  }
  this.batchMode=true;
  if (batches == null) {
    currentBatch=new ArrayList<String>();
  }
 else {
    currentBatch=batches.remove(name);
    if (currentBatch == null) {
      currentBatch=new ArrayList<String>();
    }
  }
  return true;
}
