{
  if (systemBundleControllerHolder.systemBundleController == null || systemBundleControllerHolder.systemBundleController.getState() != ServiceController.State.UP) {
    return null;
  }
 else {
    return systemBundleControllerHolder.systemBundleController.getValue().getBundleContext();
  }
}
