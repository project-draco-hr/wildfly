{
  ForkChannelService service=new ForkChannelService(channel,protocols);
  return AsynchronousService.addService(target,ChannelService.getServiceName(channel),service).addDependency(ConnectedChannelService.getServiceName(parentChannel),Channel.class,service.channel);
}
