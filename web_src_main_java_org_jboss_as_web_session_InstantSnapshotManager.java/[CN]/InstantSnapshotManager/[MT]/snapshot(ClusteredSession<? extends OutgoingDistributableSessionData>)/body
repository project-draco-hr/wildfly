{
  if (session != null) {
    try {
      getManager().storeSession(session);
    }
 catch (    Exception e) {
      getLog().warn("Failed to replicate session " + session.getIdInternal(),e);
    }
  }
}
