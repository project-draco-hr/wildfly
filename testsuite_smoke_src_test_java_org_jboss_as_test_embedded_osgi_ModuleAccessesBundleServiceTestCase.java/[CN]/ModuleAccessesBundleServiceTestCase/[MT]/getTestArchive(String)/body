{
  if (API_MODULE_NAME.equals(name))   return getAPIModuleArchive();
  if (TARGET_BUNDLE_NAME.equals(name))   return getTargetBundleArchive();
  if (CLIENT_MODULE_NAME.equals(name))   return getClientModuleArchive();
  return null;
}
