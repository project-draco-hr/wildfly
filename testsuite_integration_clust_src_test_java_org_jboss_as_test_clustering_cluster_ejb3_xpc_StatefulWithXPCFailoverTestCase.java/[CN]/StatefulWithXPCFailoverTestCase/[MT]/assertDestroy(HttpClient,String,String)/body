{
  int maxWait=GRACE_TIME;
  String name=null;
  while (maxWait > 0) {
    Thread.sleep(100);
    name=getDestroy(client,url);
    if (name != null) {
      break;
    }
    maxWait-=100;
  }
  if (name == null)   throw new AssertionError("assertDestroy timed out waiting for a result " + message);
  assertEquals(message,name,"destroy");
}
