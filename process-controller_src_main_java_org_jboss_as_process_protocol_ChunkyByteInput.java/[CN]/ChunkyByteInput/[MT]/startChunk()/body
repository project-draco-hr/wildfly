{
  final int current=input.read();
switch (current) {
case -1:
    remaining=-1;
  break;
case CHUNK_START:
remaining=readInt();
break;
case END:
remaining=-1;
finished=true;
break;
default :
throw MESSAGES.invalidStartChunk(current);
}
}
