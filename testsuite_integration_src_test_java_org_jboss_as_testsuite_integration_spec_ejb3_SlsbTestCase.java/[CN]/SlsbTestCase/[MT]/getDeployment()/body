{
  final JavaArchive archive=ShrinkWrap.create(JavaArchive.class,"slsb.jar").addClasses(GreeterCommonBusiness.class,GreeterSlsb.class,SlsbTestCase.class);
  log.info(archive.toString(true));
  return archive;
}
