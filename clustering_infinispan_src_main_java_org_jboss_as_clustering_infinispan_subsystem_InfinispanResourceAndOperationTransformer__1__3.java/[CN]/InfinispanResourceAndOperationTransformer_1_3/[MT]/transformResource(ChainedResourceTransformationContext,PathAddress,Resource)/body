{
  if (convert(resource.getModel())) {
    context.getLogger().logWarning(address,null,InfinispanMessages.MESSAGES.virtualNodesDoesNotSupportExpressions(),(String)null);
  }
  remove(resource.getModel());
}
