{
  ManagementInstallationService installer=new ManagementInstallationService();
  batchBuilder.addService(ManagementInstallationService.SERVICE_NAME,installer).addDependency(Services.JBOSS_SERVER_CONTROLLER,ServerController.class,installer.getServerControllerInjector()).setInitialMode(ServiceController.Mode.ACTIVE).install();
}
