{
  final StringBuilder b=new StringBuilder();
  int c;
  while ((c=in.read()) != -1 && c != '\n') {
    b.append((char)(c & 0xff));
  }
  if (b.length() == 0 && c == -1) {
    return null;
  }
  return b.toString();
}
