{
  final Channel channel=this.channel;
  if (channel == null) {
synchronized (this) {
      if (this.channel == null) {
        throw new IOException("Channel closed");
      }
    }
  }
  return channel;
}
