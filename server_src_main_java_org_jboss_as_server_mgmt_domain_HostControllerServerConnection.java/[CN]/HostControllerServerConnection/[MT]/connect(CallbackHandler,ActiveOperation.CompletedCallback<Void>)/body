{
  boolean ok=false;
  try {
    openChannel(callbackHandler);
    channelHandler.executeRequest(new ServerRegisterRequest(),null,callback);
    ok=true;
  }
  finally {
    if (!ok) {
      close();
    }
  }
}
