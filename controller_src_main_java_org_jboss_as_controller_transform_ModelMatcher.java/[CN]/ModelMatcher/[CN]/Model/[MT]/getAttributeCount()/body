{
  int i=attributes.size();
  List<Model> c=children;
  while (c != null && !c.isEmpty()) {
    for (    Model child : c) {
      i+=child.attributes.size();
      c=child.children;
    }
  }
  return i;
}
