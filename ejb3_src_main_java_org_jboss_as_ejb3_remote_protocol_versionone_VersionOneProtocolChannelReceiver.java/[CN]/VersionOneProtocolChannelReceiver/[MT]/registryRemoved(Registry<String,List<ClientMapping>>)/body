{
  try {
    logger.debug("Received cluster removal notification for cluster " + registry.getName());
    this.sendClusterRemovedMessage(registry);
  }
 catch (  IOException ioe) {
    logger.warn("Could not send a cluster removal message for cluster: " + registry.getName() + " to the client on channel "+ channel,ioe);
  }
}
