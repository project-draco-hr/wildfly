{
  if (unit == null)   throw new IllegalArgumentException("Null initial unit");
  DeploymentUnit parent=unit.getParent();
  while (parent != null) {
    unit=parent;
    parent=unit.getParent();
  }
  return unit;
}
