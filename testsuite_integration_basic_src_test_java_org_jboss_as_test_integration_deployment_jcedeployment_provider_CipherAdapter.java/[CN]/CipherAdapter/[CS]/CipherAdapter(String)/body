{
  if (DummyProvider.DUMMY_CIPHER.equals(cipherName))   cipherInstance=new DummyCipher();
  if (cipherInstance == null)   throw new InternalError("Unsupported cipher function " + cipherName + "!");
}
