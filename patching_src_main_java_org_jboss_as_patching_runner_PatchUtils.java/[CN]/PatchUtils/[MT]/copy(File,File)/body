{
  final FileInputStream is=new FileInputStream(source);
  try {
    byte[] backupHash=ModuleUpdateTask.copy(is,target);
    is.close();
    return backupHash;
  }
  finally {
    safeClose(is);
  }
}
