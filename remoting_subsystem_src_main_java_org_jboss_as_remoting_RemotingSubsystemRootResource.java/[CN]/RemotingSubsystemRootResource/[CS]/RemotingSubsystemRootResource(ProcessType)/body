{
  super(PATH,RemotingExtension.getResourceDescriptionResolver(RemotingExtension.SUBSYSTEM_NAME),processType.isServer() ? RemotingSubsystemAdd.SERVER : RemotingSubsystemAdd.DOMAIN,RemotingSubsystemRemove.INSTANCE);
  this.processType=processType;
}
