{
  if (model.has(SYSTEM_PROPERTY) && model.get(SYSTEM_PROPERTY).isDefined()) {
    List<Property> props=model.get(SYSTEM_PROPERTY).asPropertyList();
    if (props.size() >= 0) {
      Map<String,String> result=new HashMap<String,String>();
      for (      Property prop : props) {
        result.put(prop.getName(),prop.getValue().asString());
      }
      return result;
    }
  }
  return Collections.emptyMap();
}
