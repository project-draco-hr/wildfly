{
  if (cls == null)   throw JacORBMessages.MESSAGES.invalidNullClass();
  TypeCode ret=constantTypeCodeMap.get(cls);
  if (ret == null)   throw JacORBMessages.MESSAGES.badClassForConstant(cls.getName());
  return ret;
}
