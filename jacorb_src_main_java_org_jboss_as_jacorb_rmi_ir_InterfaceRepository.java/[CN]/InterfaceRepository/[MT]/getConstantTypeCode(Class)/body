{
  if (cls == null)   throw JacORBLogger.ROOT_LOGGER.invalidNullClass();
  TypeCode ret=constantTypeCodeMap.get(cls);
  if (ret == null)   throw JacORBLogger.ROOT_LOGGER.badClassForConstant(cls.getName());
  return ret;
}
