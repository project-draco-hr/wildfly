{
  if (cls == null)   throw JacORBLogger.ROOT_LOGGER.invalidNullClass();
  TypeCode tc=(TypeCode)typeCodeMap.get(cls);
  if (tc != null)   throw JacORBLogger.ROOT_LOGGER.duplicateTypeCodeForClass(cls.getName());
  typeCodeMap.put(cls,typeCode);
}
