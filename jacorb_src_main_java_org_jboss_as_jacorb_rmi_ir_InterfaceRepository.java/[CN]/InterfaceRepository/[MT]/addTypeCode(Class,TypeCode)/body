{
  if (cls == null)   throw JacORBMessages.MESSAGES.invalidNullClass();
  TypeCode tc=(TypeCode)typeCodeMap.get(cls);
  if (tc != null)   throw JacORBMessages.MESSAGES.duplicateTypeCodeForClass(cls.getName());
  typeCodeMap.put(cls,typeCode);
}
