{
  UserTransaction ut=getUserTransaction();
  ut.begin();
  try {
    localFind("key1").test();
    try {
      localFind("key2").test();
      fail("Only one instance of bean should be allowed by pool");
    }
 catch (    Exception e) {
    }
  }
  finally {
    ut.rollback();
  }
}
