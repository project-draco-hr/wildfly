{
  if (ModelConstants.BUNDLE.equals(element.getKey())) {
    if (hasBundle(element))     return OSGiBundleResource.INSTANCE;
    throw new NoSuchResourceException(element);
  }
 else {
    return delegate.requireChild(element);
  }
}
