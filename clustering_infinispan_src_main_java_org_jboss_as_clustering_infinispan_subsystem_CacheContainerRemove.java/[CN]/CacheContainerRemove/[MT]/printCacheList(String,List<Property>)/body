{
  System.out.println("Printing list: " + name);
  if (list != null) {
    for (    Property element : list) {
      System.out.println("element: name = " + element.getName() + ", value = "+ element.getValue());
    }
  }
 else {
    System.out.println("<empty>");
  }
}
