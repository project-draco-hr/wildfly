{
  if (driver == null)   throw new ResourceException("driver is null");
  if (connectionURL == null)   throw new ResourceException("connectionURL is null");
  return super.createConnectionFactory(cm);
}
