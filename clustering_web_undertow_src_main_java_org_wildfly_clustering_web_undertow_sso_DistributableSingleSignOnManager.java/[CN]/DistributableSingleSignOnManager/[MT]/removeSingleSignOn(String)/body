{
  Batch batch=this.manager.getBatcher().startBatch();
  SSO<Account,String,Void> sso=this.manager.findSSO(id);
  if (sso != null) {
    sso.invalidate();
    batch.close();
  }
 else {
    batch.discard();
  }
}
