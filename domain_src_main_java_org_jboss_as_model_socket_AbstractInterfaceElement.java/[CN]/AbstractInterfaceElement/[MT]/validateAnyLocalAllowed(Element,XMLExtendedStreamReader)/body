{
  if (interfaceCriteria.size() > 0)   throw new XMLStreamException(element + " cannot be combined with " + interfaceCriteria.keySet().iterator().next(),reader.getLocation());
  validateNotAnyLocal(element,reader);
}
