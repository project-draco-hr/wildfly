{
  if (address != null) {
    throw new IllegalStateException(InterfaceCriteria.class.getSimpleName() + " is not available when an IP address is configured; call " + "getIpAddress() before asking for an "+ InterfaceCriteria.class.getSimpleName());
  }
  return new OverallInterfaceCriteria();
}
