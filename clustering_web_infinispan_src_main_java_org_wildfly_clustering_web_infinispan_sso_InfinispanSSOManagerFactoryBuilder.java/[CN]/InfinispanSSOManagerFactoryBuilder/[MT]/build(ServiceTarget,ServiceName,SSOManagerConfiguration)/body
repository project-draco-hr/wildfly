{
  @SuppressWarnings("rawtypes") InjectedValue<Cache> cache=new InjectedValue<>();
  return target.addService(name,new InfinispanSSOManagerFactory(cache)).addDependency(CacheService.getServiceName(config.getContainer(),config.getCache()),Cache.class,cache);
}
