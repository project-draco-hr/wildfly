{
  if (txAttrType == TransactionAttributeType.NEVER || txAttrType == TransactionAttributeType.SUPPORTS || txAttrType == TransactionAttributeType.NOT_SUPPORTED) {
    return false;
  }
  return true;
}
