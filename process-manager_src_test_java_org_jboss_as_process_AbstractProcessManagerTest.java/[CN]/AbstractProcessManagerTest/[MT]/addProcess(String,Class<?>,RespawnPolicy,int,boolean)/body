{
  master.addProcess(processName,TestProcessUtils.createCommand(processName,clazz.getName(),debugPort,suspend),System.getenv(),".",respawnPolicy);
  processes.add(processName);
  return TestFileUtils.getOutputFile(processName);
}
