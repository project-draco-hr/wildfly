{
  master.addProcess(processName,TestProcessUtils.createCommand(processName,clazz.getName(),debugPort,suspend),System.getenv(),".");
  processes.add(processName);
  return TestFileUtils.getOutputFile(processName);
}
