def mangleCommand(self, exe):
    mangledname = re.sub('^/(usr/|)(bin|sbin)/', '', exe)
    mangledname = re.sub('[^\\w\\-\\.\\/]+', '_', mangledname)
    mangledname = re.sub('/', '.', mangledname).strip(' ._-')[0:64]
    return mangledname
