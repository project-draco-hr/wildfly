def mangle_command(command):
    mangledname = re.sub('^/(usr/|)(bin|sbin)/', '', command)
    mangledname = re.sub('[^\\w\\-\\.\\/]+', '_', mangledname)
    mangledname = re.sub('/', '.', mangledname).strip(' ._-')[0:64]
    return mangledname
