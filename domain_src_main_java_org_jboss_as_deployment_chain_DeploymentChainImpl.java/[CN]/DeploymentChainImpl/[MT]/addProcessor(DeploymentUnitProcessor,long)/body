{
  final Set<OrderedProcessor> processors=this.orderedProcessors;
  if (!processors.add(new OrderedProcessor(processor,priority)))   throw new IllegalArgumentException("Duplicate processor name and priority: " + processor.getClass().getSimpleName() + ":"+ priority);
}
