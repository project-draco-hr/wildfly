{
  if (activeBatch == null) {
    return false;
  }
  if (batches.containsKey(name)) {
    return false;
  }
  if (batches.isEmpty()) {
    batches=new HashMap<String,DefaultBatch>();
  }
  batches.put(name,activeBatch);
  activeBatch=null;
  return true;
}
