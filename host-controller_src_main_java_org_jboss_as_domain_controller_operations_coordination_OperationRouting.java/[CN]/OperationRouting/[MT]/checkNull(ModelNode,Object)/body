{
  if (toCheck == null) {
    String msg=String.format("No handler for %s at address %s",operation.require(OP).asString(),PathAddress.pathAddress(operation.get(OP_ADDR)));
    throw new OperationFailedException(new ModelNode().set(msg));
  }
}
