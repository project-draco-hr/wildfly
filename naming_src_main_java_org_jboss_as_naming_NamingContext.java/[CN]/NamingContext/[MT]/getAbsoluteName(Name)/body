{
  if (name.isEmpty()) {
    return composeName(name,prefix);
  }
 else   if ("".equals(name.get(0)) || "java:".equals(name.get(0))) {
    return name.getSuffix(1);
  }
 else   return composeName(name,prefix);
}
