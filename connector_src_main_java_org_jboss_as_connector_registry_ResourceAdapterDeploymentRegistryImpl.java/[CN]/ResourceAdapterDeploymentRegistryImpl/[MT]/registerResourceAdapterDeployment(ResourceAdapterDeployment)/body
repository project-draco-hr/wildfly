{
  if (deployment == null)   throw new IllegalArgumentException(MESSAGES.nullVar("Deployment"));
  DEPLOYMENT_CONNECTOR_REGISTRY_LOGGER.tracef("Adding deployment: %s",deployment);
  deployments.add(deployment);
}
