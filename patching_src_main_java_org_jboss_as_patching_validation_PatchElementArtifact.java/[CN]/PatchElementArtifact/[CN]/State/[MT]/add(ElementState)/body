{
switch (list.size()) {
case 0:
    list=Collections.singletonList(item);
  break;
case 1:
final List<ElementState> tmp=list;
list=new ArrayList<ElementState>();
list.add(tmp.get(0));
default :
list.add(item);
}
}
