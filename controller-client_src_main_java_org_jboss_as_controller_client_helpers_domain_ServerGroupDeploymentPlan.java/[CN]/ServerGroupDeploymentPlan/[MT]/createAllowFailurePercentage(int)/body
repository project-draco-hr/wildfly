{
  if (serverFailurePercentage < 1 || serverFailurePercentage > 99)   throw MESSAGES.invalidValue("serverFailurePercentage",serverFailurePercentage,0,100);
  return new ServerGroupDeploymentPlan(serverGroupName,true,rollingToServers,maxFailures,serverFailurePercentage);
}
