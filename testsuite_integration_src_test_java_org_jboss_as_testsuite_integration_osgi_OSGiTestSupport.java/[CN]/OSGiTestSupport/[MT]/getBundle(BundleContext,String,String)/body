{
  Bundle[] bundles=getBundles(context,symbolicName,versionRange);
  if (bundles == null)   return null;
  if (bundles.length != 1)   throw new IllegalStateException("Cannot obtain a single bundle, found: " + Arrays.asList(bundles));
  return bundles[0];
}
