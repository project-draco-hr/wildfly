{
  if (serverFailures < 1)   throw new IllegalArgumentException(String.format("Illegal serverFailures value %s -- must be greater than zero",serverFailures));
  return new ServerGroupDeploymentPlan(serverGroupName,true,rollingToServers,serverFailures,maxFailurePercentage);
}
