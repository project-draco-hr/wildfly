{
  if (serverFailurePercentage < 1 || serverFailurePercentage > 99)   throw new IllegalArgumentException(String.format("Illegal serverFailures vaue %s -- must be greater than zero and less than 100 ",serverFailurePercentage));
  return new ServerGroupDeploymentPlan(serverGroupName,true,rollingToServers,maxFailures,serverFailurePercentage);
}
