{
  return new LdapSearcher<LdapEntry,String>(){
    @Override public LdapEntry search(    DirContext dirContext,    String suppliedName){
      return new LdapEntry(suppliedName,suppliedName);
    }
  }
;
}
