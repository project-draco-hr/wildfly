{
  AccessController.doPrivileged(new PrivilegedAction(){
    public Object run(){
      SecurityContext sc=getSecurityContext();
      if (sc == null)       throw MESSAGES.securityContextIsNull();
      sc.getUtil().createSubjectInfo(p,cred,s);
      return null;
    }
  }
);
}
