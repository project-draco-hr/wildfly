{
  T result=null;
  try {
    result=ManagementRequest.this.readResponse(input);
    future.done(result);
    return result;
  }
 catch (  Exception e) {
    future.failed(e);
  }
 finally {
    clientChannelStrategy.requestDone();
  }
  return result;
}
