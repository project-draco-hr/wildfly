{
  if (deploymentElement != null) {
    startStopHandler.undeploy(toReplace,container,resultHandler,param);
    startStopHandler.deploy(newDeployment,deploymentElement.getRuntimeName(),deploymentElement.getSha1Hash(),container,resultHandler,param);
  }
 else   if (resultHandler != null) {
    throw new IllegalStateException("Unknown deployment " + newDeployment);
  }
}
