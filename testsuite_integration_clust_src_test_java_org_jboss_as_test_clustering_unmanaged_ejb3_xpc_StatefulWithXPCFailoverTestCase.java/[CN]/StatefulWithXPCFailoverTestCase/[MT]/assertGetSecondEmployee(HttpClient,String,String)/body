{
  int maxWait=GRACE_TIME;
  String name=null;
  while (maxWait > 0) {
    Thread.sleep(1000);
    name=getSecondEmployee(client,url);
    if (name != null) {
      break;
    }
    maxWait-=1000;
  }
  if (name == null)   throw new AssertionError("assertGetSecondEmployee Timed out waiting for a result");
  assertEquals(message,name,"Tom Brady");
}
