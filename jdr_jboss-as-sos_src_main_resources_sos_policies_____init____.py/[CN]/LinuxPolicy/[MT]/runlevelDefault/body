def runlevelDefault(self):
    try:
        with open('/etc/inittab') as fp:
            pattern = 'id:(\\d{1}):initdefault:'
            text = fp.read()
            return int(re.findall(pattern, text)[0])
    except:
        return 3
