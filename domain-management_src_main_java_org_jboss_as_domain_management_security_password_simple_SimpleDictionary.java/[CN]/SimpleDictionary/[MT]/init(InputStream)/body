{
  try {
    Properties props=new Properties();
    props.load(is);
    Iterator it=props.keySet().iterator();
    while (it.hasNext()) {
      String word=(String)it.next();
      words.add(word);
    }
  }
 catch (  Exception ioe) {
    ioe.printStackTrace();
  }
}
