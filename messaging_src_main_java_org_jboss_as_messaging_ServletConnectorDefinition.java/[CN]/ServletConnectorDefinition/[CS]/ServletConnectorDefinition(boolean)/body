{
  super(PATH,new StandardResourceDescriptionResolver(CommonAttributes.CONNECTOR,MessagingExtension.RESOURCE_NAME,MessagingExtension.class.getClassLoader(),true,false){
    @Override public String getResourceDescription(    Locale locale,    ResourceBundle bundle){
      return bundle.getString(SERVLET_CONNECTOR);
    }
  }
,ServletConnectorAdd.INSTANCE,ServletConnectorRemove.INSTANCE);
  this.registerRuntimeOnly=registerRuntimeOnly;
}
