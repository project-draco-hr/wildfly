{
  final ProcessManagerClient value=client.getValue();
  stop=true;
  if (value != null) {
    context.asynchronous();
    stopContext=context;
    StreamUtils.safeClose(value);
    client.uninject();
  }
}
