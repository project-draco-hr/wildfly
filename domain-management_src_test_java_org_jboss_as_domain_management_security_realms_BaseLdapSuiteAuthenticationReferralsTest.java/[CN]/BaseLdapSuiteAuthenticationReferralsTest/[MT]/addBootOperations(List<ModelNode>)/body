{
  super.addBootOperations(bootOperations);
  bootOperations.add(SecurityRealmAddBuilder.builder(SLAVE_REALM).authentication().ldap().setConnection(SLAVE_CONNECTION_NAME).setBaseDn(BASE_DN).setUsernameFilter(USERNAME_FILTER).build().build().build());
}
