{
  if (toCombine != null) {
    for (    JvmOptionsElement pe : toCombine) {
      if (pe == null)       continue;
      for (      String value : pe.getOptions()) {
        addOption(value);
      }
    }
  }
}
