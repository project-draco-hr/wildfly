{
  if (file.isDirectory()) {
    for (    String name : file.list()) {
      File current=new File(file,name);
      if (current.isDirectory()) {
        cleanFiles(current);
      }
    }
  }
  file.delete();
}
