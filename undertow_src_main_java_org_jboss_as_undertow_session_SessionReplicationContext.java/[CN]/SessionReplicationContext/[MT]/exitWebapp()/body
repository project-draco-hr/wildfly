{
  SessionReplicationContext ctx=getCurrentContext();
  if (ctx != null) {
    ctx.webappCount--;
    if (ctx.webappCount < 1) {
      ctx.exchange=null;
      replicationContext.set(null);
      return ctx;
    }
  }
  return EMPTY;
}
