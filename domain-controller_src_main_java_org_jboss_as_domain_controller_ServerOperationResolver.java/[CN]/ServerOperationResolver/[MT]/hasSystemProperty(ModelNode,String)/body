{
  boolean result=false;
  if (resource.hasDefined(SYSTEM_PROPERTIES)) {
    for (    Property prop : resource.get(SYSTEM_PROPERTIES).asPropertyList()) {
      if (propName.equals(prop.getName()))       return true;
    }
  }
  return result;
}
