{
  final List<ModelNode> list=new ArrayList<>();
  list.add(getUndefineAttributeOperation(subsystem.append("strict-max-bean-instance-pool","slsb-strict-max-pool"),"derive-size"));
  list.add(getUndefineAttributeOperation(subsystem.append("strict-max-bean-instance-pool","mdb-strict-max-pool"),"derive-size"));
  return list;
}
