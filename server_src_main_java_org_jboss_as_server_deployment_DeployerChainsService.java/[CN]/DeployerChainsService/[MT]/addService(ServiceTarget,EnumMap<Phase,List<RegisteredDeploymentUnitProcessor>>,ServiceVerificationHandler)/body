{
  final DeployerChains deployerChains=new DeployerChains(phases);
  serviceTarget.addService(Services.JBOSS_DEPLOYMENT_CHAINS,new DeployerChainsService(deployerChains)).addListener(verificationHandler).install();
}
