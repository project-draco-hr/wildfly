{
  String subsystemXml="<subsystem xmlns=\"" + Namespace.JMX_1_0.getUriString() + "\" show-model=\"true\">"+ "    <jmx-connector registry-binding=\"registry1\" server-binding=\"server1\" />"+ "</subsystem>";
  try {
    super.parse(subsystemXml);
    Assert.fail("Should not have parsed bad attribute");
  }
 catch (  XMLStreamException expected) {
  }
}
