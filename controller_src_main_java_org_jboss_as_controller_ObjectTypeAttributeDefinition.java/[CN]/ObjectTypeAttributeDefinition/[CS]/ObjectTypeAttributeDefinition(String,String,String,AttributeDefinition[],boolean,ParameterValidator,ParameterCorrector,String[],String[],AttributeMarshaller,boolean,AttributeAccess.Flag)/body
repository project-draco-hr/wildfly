{
  super(name,xmlName,null,ModelType.OBJECT,allowNull,false,null,corrector,validator,false,alternatives,requires,attributeMarshaller,resourceOnly,flags);
  this.valueTypes=valueTypes;
  if (suffix == null) {
    this.suffix="";
  }
 else {
    this.suffix=suffix;
  }
}
