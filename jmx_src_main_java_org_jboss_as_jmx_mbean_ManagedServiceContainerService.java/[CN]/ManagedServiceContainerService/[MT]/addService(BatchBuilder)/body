{
  ManagedServiceContainerService service=new ManagedServiceContainerService();
  batchBuilder.addService(SERVICE_NAME,service).addDependency(MBeanServerService.SERVICE_NAME,MBeanServer.class,service.injectedMBeanServer).setInitialMode(Mode.ACTIVE).install();
}
