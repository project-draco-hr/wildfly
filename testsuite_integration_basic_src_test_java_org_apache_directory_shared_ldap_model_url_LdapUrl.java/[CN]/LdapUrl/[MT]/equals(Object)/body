{
  if (this == obj) {
    return true;
  }
  if (obj == null) {
    return false;
  }
  if (getClass() != obj.getClass()) {
    return false;
  }
  final LdapUrl other=(LdapUrl)obj;
  return this.toString().equals(other.toString());
}
