{
  if (wrapper != null) {
    if (name == null) {
      if (buf != null && buf.length() > 0) {
        wrapper.add(getStringValue());
      }
    }
 else {
      addChild(wrapper,name,getStringValue());
      name=null;
    }
  }
}
