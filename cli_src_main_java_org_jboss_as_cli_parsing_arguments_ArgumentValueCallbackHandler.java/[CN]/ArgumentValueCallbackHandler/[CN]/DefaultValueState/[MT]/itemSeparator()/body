{
  if (buf.length() == 0) {
    return;
  }
  if (wrapper == null) {
    wrapper=new ModelNode();
  }
  if (name == null) {
    wrapper.add(getStringValue());
  }
 else {
    addChild(wrapper,name,getStringValue());
    name=null;
  }
  buf.setLength(0);
}
