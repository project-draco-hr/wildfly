{
  if (timerState == TimerState.EXPIRED)   throw EjbLogger.ROOT_LOGGER.timerHasExpired();
  if (timerState == TimerState.CANCELED)   throw EjbLogger.ROOT_LOGGER.timerWasCanceled();
  AllowedMethodsInformation.checkAllowed(MethodType.TIMER_SERVICE_METHOD);
}
