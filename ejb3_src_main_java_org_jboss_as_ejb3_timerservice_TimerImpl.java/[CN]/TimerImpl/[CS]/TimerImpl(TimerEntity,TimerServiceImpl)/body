{
  this(persistedTimer.getId(),service,persistedTimer.getInitialDate(),persistedTimer.getInterval(),persistedTimer.getNextDate(),persistedTimer.getInfo(),true,persistedTimer.getPrimaryKey());
  this.previousRun=persistedTimer.getPreviousRun();
  this.timerState=persistedTimer.getTimerState();
}
