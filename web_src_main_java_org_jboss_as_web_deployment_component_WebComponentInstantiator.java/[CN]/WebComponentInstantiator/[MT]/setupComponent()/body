{
  if (component == null) {
synchronized (this) {
      if (component == null) {
        component=((ServiceController<BasicComponent>)serviceRegistry.getRequiredService(serviceName)).getValue();
      }
    }
  }
}
