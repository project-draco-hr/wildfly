{
  if (serviceRegistry == null)   throw new IllegalArgumentException("serviceRegistry is null");
  if (serviceTarget == null)   throw new IllegalArgumentException("serviceTarget is null");
  this.serviceRegistry=serviceRegistry;
  this.serviceTarget=serviceTarget;
}
