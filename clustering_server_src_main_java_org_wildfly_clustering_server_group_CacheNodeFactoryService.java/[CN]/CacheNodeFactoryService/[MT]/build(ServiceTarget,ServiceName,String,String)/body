{
  CacheNodeFactoryService service=new CacheNodeFactoryService();
  return target.addService(name,service).addDependency(ChannelNodeFactoryProvider.getServiceName(containerName),ChannelNodeFactory.class,service.factory);
}
