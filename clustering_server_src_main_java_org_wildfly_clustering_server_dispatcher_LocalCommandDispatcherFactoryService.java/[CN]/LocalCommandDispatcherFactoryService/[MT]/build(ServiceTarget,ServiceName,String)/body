{
  LocalCommandDispatcherFactoryService service=new LocalCommandDispatcherFactoryService();
  return target.addService(name,service).addDependency(ChannelGroupProvider.getServiceName(cluster),Group.class,service.group);
}
