{
  LocalCommandDispatcherFactoryService service=new LocalCommandDispatcherFactoryService();
  return target.addService(name,service).addDependency(GroupServiceNames.GROUP.getServiceName(cluster),Group.class,service.group);
}
