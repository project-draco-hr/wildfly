{
  LocalCommandDispatcherFactoryService service=new LocalCommandDispatcherFactoryService();
  return target.addService(name,service).addDependency(ChannelServiceNames.GROUP.getServiceName(cluster),Group.class,service.group);
}
