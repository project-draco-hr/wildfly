{
  while (dsCount > 0) {
    remove(getDsAddress(dsCount,false));
    reload();
    dsCount--;
  }
  while (xaDsCount > 0) {
    remove(getDsAddress(xaDsCount,true));
    reload();
    xaDsCount--;
  }
}
