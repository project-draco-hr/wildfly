{
  super(name,xmlName,suffix,valueTypes,allowNull,validator,corrector,alternatives,requires,attributeMarshaller,resourceOnly,deprecationData,accessConstraints,nullSignificant,parser,flags);
  this.propertyName=propertyName;
  this.resolver=resolver;
}
