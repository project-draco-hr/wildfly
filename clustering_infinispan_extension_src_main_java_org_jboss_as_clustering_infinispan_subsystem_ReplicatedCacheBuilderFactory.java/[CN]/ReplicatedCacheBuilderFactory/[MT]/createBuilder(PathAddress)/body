{
  String containerName=address.getParent().getLastElement().getValue();
  String cacheName=address.getLastElement().getValue();
  return new SharedStateCacheBuilder(containerName,cacheName,CacheMode.REPL_SYNC);
}
