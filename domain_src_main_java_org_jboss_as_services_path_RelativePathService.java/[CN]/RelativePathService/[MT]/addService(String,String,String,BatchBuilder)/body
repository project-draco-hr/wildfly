{
  ServiceName sname=getPathServiceName(name);
  RelativePathService service=new RelativePathService(relativePath);
  batchBuilder.addService(sname,service).addDependency(getPathServiceName(relativeTo),String.class,service.injectedPath);
}
