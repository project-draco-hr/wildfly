{
  ServiceName sname=pathNameOf(name);
  RelativePathService service=new RelativePathService(relativePath);
  return batchBuilder.addService(sname,service).addDependency(pathNameOf(relativeTo),String.class,service.injectedPath);
}
