{
  if (System.getSecurityManager() == null)   return System.setProperty(key,value);
  return doPrivileged(new PrivilegedAction<String>(){
    @Override public String run(){
      return System.setProperty(key,value);
    }
  }
);
}
