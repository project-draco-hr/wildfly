{
  return new TransformationDescriptionBuilder(){
    @Override public TransformationDescriptionBuilder redirectTo(    PathElement element){
      return this;
    }
    @Override public TransformationDescription build(){
      return new DiscardDefinition(pathElement);
    }
  }
;
}
