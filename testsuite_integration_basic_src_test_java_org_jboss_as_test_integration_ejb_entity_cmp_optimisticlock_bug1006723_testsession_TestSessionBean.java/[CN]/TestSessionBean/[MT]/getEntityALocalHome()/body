{
  if (entityALocalHome == null) {
    try {
      entityALocalHome=(EntityALocalHome)getContext().lookup("java:module/EntityA!org.jboss.as.test.integration.ejb.entity.cmp.optimisticlock.bug1006723.testentity.EntityALocalHome");
    }
 catch (    NamingException e) {
      throw new EJBException(e.getMessage());
    }
  }
  return entityALocalHome;
}
