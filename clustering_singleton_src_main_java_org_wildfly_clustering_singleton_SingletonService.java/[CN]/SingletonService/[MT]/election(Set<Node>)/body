{
  SingletonElectionPolicy policy=this.electionPolicy;
  List<Node> nodes=this.dispatcherFactory.getValue().getNodes();
  nodes.retainAll(candidates);
  return !nodes.isEmpty() ? policy.elect(nodes) : null;
}
