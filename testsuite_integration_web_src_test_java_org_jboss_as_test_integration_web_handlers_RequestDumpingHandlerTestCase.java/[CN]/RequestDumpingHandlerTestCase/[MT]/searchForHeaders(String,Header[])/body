{
  if (hdrs != null) {
    for (    Header hdr : hdrs) {
      searchInFile(content,"\\s+header=" + hdr.getName() + "="+ hdr.getValue());
    }
  }
 else {
    searchInFile(content,"\\s+header=",false);
  }
}
