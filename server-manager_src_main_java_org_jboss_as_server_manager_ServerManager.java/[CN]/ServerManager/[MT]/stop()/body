{
  for (  Map.Entry<String,Server> entry : servers.entrySet()) {
    try {
      entry.getValue().stop();
      processManagerSlave.removeProcess(entry.getKey());
    }
 catch (    Exception e) {
    }
  }
}
