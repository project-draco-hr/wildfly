{
  log.debugf("Stopping OSGi BundleManager");
  try {
    if (moduleLoaderInjector != null)     moduleLoaderInjector.uninject();
    bundleManager=null;
  }
 catch (  Exception ex) {
    log.errorf(ex,"Cannot stop OSGi BundleManager");
  }
}
