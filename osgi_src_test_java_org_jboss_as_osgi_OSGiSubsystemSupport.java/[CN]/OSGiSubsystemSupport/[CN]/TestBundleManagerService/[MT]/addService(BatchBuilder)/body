{
  TestBundleManagerService service=new TestBundleManagerService();
  batchBuilder.addService(BundleManagerService.SERVICE_NAME,service).addDependency(ClassifyingModuleLoaderService.SERVICE_NAME,ClassifyingModuleLoaderService.class,service.injectedModuleLoader).addDependency(Configuration.SERVICE_NAME,Configuration.class,service.injectedConfig).install();
}
