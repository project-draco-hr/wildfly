{
  batchBuilder.addService(TestServerDeploymentRepository.SERVICE_NAME,new TestServerDeploymentRepository()).install();
  deploymentChain=new DeploymentChainImpl();
  batchBuilder.addService(DeploymentChain.SERVICE_NAME,new DeploymentChainService(deploymentChain)).install();
  OSGiDeploymentService.enableListener=false;
}
