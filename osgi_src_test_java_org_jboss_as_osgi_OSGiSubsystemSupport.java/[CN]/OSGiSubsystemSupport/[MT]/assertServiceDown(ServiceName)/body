{
  ServiceController<?> controller=serviceContainer.getService(serviceName);
  if (controller == null)   return;
  if (controller.getState() != State.DOWN)   fail("Unexpected service state: " + controller.getState());
}
