{
  AnnotationValue value=annotationInstance.value();
  if (value == null) {
    return null;
  }
  if (replacement)   return PropertiesValueResolver.replaceProperties(value.asString());
 else   return value.asString();
}
