{
  if (cls == null)   throw ORBMessages.MESSAGES.invalidNullClass();
  TypeCode ret=constantTypeCodeMap.get(cls);
  if (ret == null)   throw ORBMessages.MESSAGES.badClassForConstant(cls.getName());
  return ret;
}
