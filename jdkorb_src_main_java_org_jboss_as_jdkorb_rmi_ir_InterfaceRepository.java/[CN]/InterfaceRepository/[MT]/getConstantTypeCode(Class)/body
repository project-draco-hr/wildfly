{
  if (cls == null)   throw JdkORBMessages.MESSAGES.invalidNullClass();
  TypeCode ret=constantTypeCodeMap.get(cls);
  if (ret == null)   throw JdkORBMessages.MESSAGES.badClassForConstant(cls.getName());
  return ret;
}
