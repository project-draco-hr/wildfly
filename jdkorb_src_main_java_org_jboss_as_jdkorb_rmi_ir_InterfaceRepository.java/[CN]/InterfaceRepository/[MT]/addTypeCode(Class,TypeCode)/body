{
  if (cls == null)   throw ORBMessages.MESSAGES.invalidNullClass();
  TypeCode tc=(TypeCode)typeCodeMap.get(cls);
  if (tc != null)   throw ORBMessages.MESSAGES.duplicateTypeCodeForClass(cls.getName());
  typeCodeMap.put(cls,typeCode);
}
