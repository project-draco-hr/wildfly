{
  if (cls == null)   throw JdkORBMessages.MESSAGES.invalidNullClass();
  TypeCode tc=(TypeCode)typeCodeMap.get(cls);
  if (tc != null)   throw JdkORBMessages.MESSAGES.duplicateTypeCodeForClass(cls.getName());
  typeCodeMap.put(cls,typeCode);
}
