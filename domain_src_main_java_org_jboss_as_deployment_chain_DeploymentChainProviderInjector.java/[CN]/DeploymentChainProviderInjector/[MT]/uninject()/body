{
  final DeploymentChain deploymentChain=deploymentChainValue.getValue();
  if (deploymentChainProvider != null) {
    deploymentChainProvider.removeDeploymentChain(deploymentChain,selector,priority);
    deploymentChainProvider=null;
  }
}
