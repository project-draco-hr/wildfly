{
  if (deployment == null)   throw new IllegalArgumentException("Deployment is null");
  if (!ironJacamar.containsKey(deployment))   throw new NotFoundException(deployment + " isn't registered");
  return ironJacamar.get(deployment);
}
