{
  if (deployment == null)   throw new IllegalArgumentException("Deployment is null");
  if (!raRoots.containsKey(deployment))   throw new NotFoundException(deployment + " isn't registered");
  return raRoots.get(deployment);
}
