{
  if (deployment == null)   throw new IllegalArgumentException("Deployment is null");
  if (!raTemplates.containsKey(deployment))   throw new NotFoundException(deployment + " isn't registered");
  raTemplates.remove(deployment);
  raRoots.remove(deployment);
  ironJacamar.remove(deployment);
}
