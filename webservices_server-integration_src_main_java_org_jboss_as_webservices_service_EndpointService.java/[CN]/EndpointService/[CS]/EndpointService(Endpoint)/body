{
  this.endpoint=endpoint;
  final ObjectName on=endpoint.getName();
  this.name=WSServices.ENDPOINT_SERVICE.append(on.getKeyProperty(Endpoint.SEPID_PROPERTY_CONTEXT)).append(on.getKeyProperty(Endpoint.SEPID_PROPERTY_ENDPOINT));
}
