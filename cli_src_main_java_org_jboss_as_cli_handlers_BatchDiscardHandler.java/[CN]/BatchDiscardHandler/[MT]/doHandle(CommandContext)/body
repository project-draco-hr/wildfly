{
  String name=null;
  if (ctx.hasArguments()) {
    name=ctx.getArguments().get(0);
  }
  if (!ctx.discardBatch(name)) {
    if (name == null) {
      ctx.printLine("There is no unnamed batch to discard.");
    }
 else {
      ctx.printLine("Batch '" + name + "' doesn't exist.");
    }
  }
}
