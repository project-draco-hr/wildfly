{
  if (head == null) {
    head=row;
    tail=row;
  }
 else   if (row == head) {
  }
 else   if (row == tail) {
    tail=row.prev;
    tail.next=null;
    row.prev=null;
    row.next=head;
    head.prev=row;
    head=row;
  }
 else {
    CachedRow next=row.next;
    CachedRow prev=row.prev;
    if (prev != null) {
      prev.next=next;
    }
    if (next != null) {
      next.prev=prev;
    }
    head.prev=row;
    row.next=head;
    row.prev=null;
    head=row;
  }
}
