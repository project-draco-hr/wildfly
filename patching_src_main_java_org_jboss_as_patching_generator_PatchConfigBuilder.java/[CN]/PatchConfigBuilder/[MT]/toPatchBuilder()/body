{
  PatchBuilder pb=PatchBuilder.create().setPatchId(getPatchId()).setDescription(getDescription()).setPatchType(getPatchType()).setResultingVersion(getResultingVersion());
  for (  String applyTo : appliesTo) {
    pb.addAppliesTo(applyTo);
  }
  return pb;
}
