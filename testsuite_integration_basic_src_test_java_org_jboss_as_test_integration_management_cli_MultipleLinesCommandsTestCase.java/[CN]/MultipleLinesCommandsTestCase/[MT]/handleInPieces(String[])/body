{
  final CommandContext ctx=CLITestUtil.getCommandContext();
  try {
    ctx.connectController();
    for (    String line : arr) {
      ctx.handle(line);
    }
  }
  finally {
    ctx.terminateSession();
  }
}
