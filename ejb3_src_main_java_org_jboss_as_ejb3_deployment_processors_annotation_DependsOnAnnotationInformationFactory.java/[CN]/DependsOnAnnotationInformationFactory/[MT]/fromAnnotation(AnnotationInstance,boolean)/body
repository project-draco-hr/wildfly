{
  if (replacement) {
    String[] values=annotationInstance.value().asStringArray();
    for (int i=0; i < values.length; i++)     values[i]=PropertiesValueResolver.replaceProperties(values[i]);
    return values;
  }
 else {
    return annotationInstance.value().asStringArray();
  }
}
