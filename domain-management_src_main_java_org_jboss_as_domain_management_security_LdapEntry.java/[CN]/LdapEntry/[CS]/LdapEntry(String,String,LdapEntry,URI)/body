{
  this.simpleName=simpleName;
  this.distinguishedName=distinguishedName;
  this.previousEntry=previousEntry;
  this.referralURI=referralURI;
  hashCode=(simpleName == null ? 7 : simpleName.hashCode()) * (distinguishedName == null ? 31 : distinguishedName.hashCode() * (previousEntry == null ? 37 : previousEntry.hashCode) * (referralURI == null ? 41 : referralURI.hashCode()));
}
