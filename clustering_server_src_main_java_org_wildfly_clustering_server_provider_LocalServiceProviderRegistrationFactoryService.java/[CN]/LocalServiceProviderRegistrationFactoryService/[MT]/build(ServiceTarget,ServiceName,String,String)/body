{
  LocalServiceProviderRegistrationFactoryService service=new LocalServiceProviderRegistrationFactoryService();
  return target.addService(name,service).addDependency(CacheGroupProvider.getServiceName(containerName,cacheName),Group.class,service.group);
}
