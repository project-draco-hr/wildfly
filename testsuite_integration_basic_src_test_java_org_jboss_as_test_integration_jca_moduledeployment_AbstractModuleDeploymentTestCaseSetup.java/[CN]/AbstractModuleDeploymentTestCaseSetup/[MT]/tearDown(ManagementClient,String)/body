{
  try {
    remove(address,managementClient);
  }
  finally {
    removeModule(defaultPath,true);
  }
  if (reloadRequired) {
    executeReload();
  }
  for (  Path p : toRemove) {
    deleteRecursively(p);
  }
  toRemove.clear();
}
