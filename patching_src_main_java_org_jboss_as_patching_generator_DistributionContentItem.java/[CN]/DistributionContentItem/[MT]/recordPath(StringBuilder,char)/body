{
  if (parent != null) {
    parent.recordPath(sb,delimiter);
    if (parent.getType() != Type.DISTRIBUTION_ROOT) {
      sb.append(delimiter);
    }
  }
  if (name != null) {
    sb.append(name);
  }
}
