{
  SessionBeanComponentDescription.SessionBeanType sessionBeanType=source.getSessionBeanType();
switch (sessionBeanType) {
case STATELESS:
    return new StatelessComponentDescription(source.getComponentName(),source.getComponentClassName(),source.getModuleName(),source.getApplicationName());
case STATEFUL:
  return new StatefulComponentDescription(source.getComponentName(),source.getComponentClassName(),source.getModuleName(),source.getApplicationName());
case SINGLETON:
return new SingletonComponentDescription(source.getComponentName(),source.getComponentClassName(),source.getModuleName(),source.getApplicationName());
default :
throw new IllegalArgumentException("Unknown session bean type: " + sessionBeanType + " for bean "+ source.getEJBName());
}
}
