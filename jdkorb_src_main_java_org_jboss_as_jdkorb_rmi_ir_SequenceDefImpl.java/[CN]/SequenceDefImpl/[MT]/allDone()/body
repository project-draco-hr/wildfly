{
  try {
    element_type_def=IDLTypeImpl.getIDLType(type().content_type(),repository);
  }
 catch (  BadKind ex) {
    throw ORBMessages.MESSAGES.badKindForTypeCode(type().kind().value());
  }
  getReference();
}
