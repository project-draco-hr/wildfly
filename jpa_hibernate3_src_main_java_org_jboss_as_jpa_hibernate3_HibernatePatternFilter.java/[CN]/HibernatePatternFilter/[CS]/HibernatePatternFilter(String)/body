{
  if (pattern == null)   throw new IllegalArgumentException("Null pattern");
  exact=pattern.contains("/") == false;
  if (exact == false && (pattern.startsWith("**/*"))) {
    this.pattern=pattern.substring(4);
  }
 else {
    this.pattern=pattern;
  }
}
