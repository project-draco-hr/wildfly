{
  if (iface == null)   throw new IllegalArgumentException("Null interface");
  Object wrapped=getWrappedObject();
  if (wrapped == null)   return false;
  return iface.isAssignableFrom(wrapped.getClass());
}
