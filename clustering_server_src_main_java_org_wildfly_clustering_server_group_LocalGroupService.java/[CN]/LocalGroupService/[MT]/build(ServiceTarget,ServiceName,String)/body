{
  LocalGroupService service=new LocalGroupService(group);
  return target.addService(name,service).addDependency(GroupServiceNames.NODE_FACTORY.getServiceName(group),JGroupsNodeFactory.class,service.factory);
}
