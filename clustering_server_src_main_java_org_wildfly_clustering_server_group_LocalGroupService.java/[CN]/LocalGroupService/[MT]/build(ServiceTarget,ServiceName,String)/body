{
  LocalGroupService service=new LocalGroupService(cluster);
  return target.addService(name,service).addDependency(ChannelNodeFactoryProvider.getServiceName(cluster),ChannelNodeFactory.class,service.factory);
}
