{
  ChannelService service=new ChannelService(id);
  return AsynchronousService.addService(target,getServiceName(id),service).addDependency(ChannelFactoryService.getServiceName(stack),ChannelFactory.class,service.factory).addDependency(MBeanServerService.SERVICE_NAME,MBeanServer.class,service.server);
}
