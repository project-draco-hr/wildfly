{
  if (contained.getRepository() != delegateFor.getRepository())   throw ORBMessages.MESSAGES.wrongInterfaceRepository();
  if (contMap.get(name) != null)   throw ORBMessages.MESSAGES.duplicateRepositoryName();
  cont.add(contained);
  contMap.put(name,contained);
}
