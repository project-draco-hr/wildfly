{
  if (contained.getRepository() != delegateFor.getRepository())   throw JdkORBMessages.MESSAGES.wrongInterfaceRepository();
  if (contMap.get(name) != null)   throw JdkORBMessages.MESSAGES.duplicateRepositoryName();
  cont.add(contained);
  contMap.put(name,contained);
}
