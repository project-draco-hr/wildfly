{
  serviceTarget.addService(SERVICE_NAME,new ManagementChannelRegistryService()).addDependency(ManagementRemotingServices.MANAGEMENT_ENDPOINT).setInitialMode(ServiceController.Mode.ACTIVE).install();
}
