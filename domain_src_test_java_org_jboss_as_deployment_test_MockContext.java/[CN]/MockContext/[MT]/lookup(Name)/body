{
  if (name.isEmpty())   return new MockContext(prefix);
  final Name absoluteName=getAbsoluteName(name);
  final Object result=entries.get(absoluteName);
  if (result == null)   throw new NameNotFoundException(absoluteName.toString());
  return result;
}
