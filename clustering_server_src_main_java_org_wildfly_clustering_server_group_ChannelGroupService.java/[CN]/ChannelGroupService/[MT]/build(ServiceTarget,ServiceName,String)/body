{
  ChannelGroupService service=new ChannelGroupService();
  return target.addService(name,service).addDependency(GlobalComponentRegistryService.getServiceName(cluster)).addDependency(EmbeddedCacheManagerService.getServiceName(cluster),EmbeddedCacheManager.class,service.manager).addDependency(ChannelServiceNames.NODE_FACTORY.getServiceName(cluster),ChannelNodeFactory.class,service.factory);
}
