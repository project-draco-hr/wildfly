{
  ChannelGroupService service=new ChannelGroupService();
  return target.addService(name,service).addDependency(GroupServiceNames.COMMAND_DISPATCHER.getServiceName(group),CommandDispatcherFactory.class,service.factory);
}
