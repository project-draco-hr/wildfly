{
  List<ValidationProvider<?>> providers=loadProviders(SecurityActions.getThreadContextClassLoader());
  if (providers != null && !providers.isEmpty()) {
    return providers;
  }
 else {
    return loadProviders(SecurityActions.getClassLoader(WildFlyProviderResolver.class));
  }
}
