{
  super(javaName);
  if (type == Void.TYPE || !type.isPrimitive() && type != java.lang.String.class)   throw new IllegalArgumentException("Bad type for constant: " + type.getName());
  this.type=type;
  this.value=value;
}
