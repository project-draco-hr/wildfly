{
  super(javaName);
  if (type == Void.TYPE || !type.isPrimitive() && type != java.lang.String.class)   throw JacORBMessages.MESSAGES.badConstantType(type.getName());
  this.type=type;
  this.value=value;
}
