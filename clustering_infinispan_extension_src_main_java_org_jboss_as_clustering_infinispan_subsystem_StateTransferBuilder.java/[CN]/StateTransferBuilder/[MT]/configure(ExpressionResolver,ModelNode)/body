{
  this.builder.chunkSize(CHUNK_SIZE.getDefinition().resolveModelAttribute(resolver,model).asInt()).fetchInMemoryState(true).timeout(TIMEOUT.getDefinition().resolveModelAttribute(resolver,model).asLong());
  return this;
}
