{
  final ThreadLocal<Boolean> checking=WildFlySecurityManager.CHECKING;
  if (checking.get() == TRUE) {
    return action.run();
  }
  checking.set(TRUE);
  try {
    return action.run();
  }
  finally {
    checking.set(FALSE);
  }
}
