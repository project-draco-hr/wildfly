{
  if (source.exists()) {
    final FileInputStream is=new FileInputStream(source);
    try {
      backupHash=copy(is,target);
      is.close();
    }
  finally {
      PatchUtils.safeClose(is);
    }
  }
}
