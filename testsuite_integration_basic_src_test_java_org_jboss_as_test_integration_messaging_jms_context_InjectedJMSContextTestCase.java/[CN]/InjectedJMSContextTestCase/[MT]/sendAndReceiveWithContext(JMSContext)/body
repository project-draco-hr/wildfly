{
  String text=UUID.randomUUID().toString();
  TemporaryQueue tempQueue=ctx.createTemporaryQueue();
  ctx.createProducer().send(tempQueue,text);
  assertMessageIsReceived(tempQueue,ctx,text,false);
}
