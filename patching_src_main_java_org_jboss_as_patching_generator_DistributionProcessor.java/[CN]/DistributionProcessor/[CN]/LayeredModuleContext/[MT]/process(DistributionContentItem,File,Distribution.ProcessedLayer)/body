{
  processModules(parent,layerDir,new ModuleContext(){
    @Override public void addModule(    DistributionContentItem module){
      processedLayer.addModule(module);
    }
    @Override public boolean isIgnored(    DistributionContentItem item){
      return distribution.isIgnored(item);
    }
  }
);
}
