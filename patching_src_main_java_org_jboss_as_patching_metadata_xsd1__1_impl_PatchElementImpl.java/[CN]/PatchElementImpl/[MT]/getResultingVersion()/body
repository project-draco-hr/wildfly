{
  if (patchType == null) {
    return null;
  }
  if (patchType == PatchType.ONE_OFF) {
    if (provider == null) {
      return null;
    }
    return provider.getVersion();
  }
  return resultingVersion;
}
