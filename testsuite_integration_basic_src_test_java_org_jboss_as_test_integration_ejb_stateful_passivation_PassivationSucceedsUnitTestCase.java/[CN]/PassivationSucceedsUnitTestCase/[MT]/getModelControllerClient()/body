{
  if (client == null) {
    client=ModelControllerClient.Factory.create(InetAddress.getByName(HostnameResolver.getHostname()),HostnameResolver.getManagementPort(),Authentication.getCallbackHandler());
  }
  return client;
}
