{
  ModelNode op=Util.getEmptyOperation("composite",new ModelNode());
  ModelNode steps=op.get("steps");
  for (  ModelNode update : updates) {
    steps.add(update);
  }
  return ExecutionContextBuilder.Factory.create(op).build();
}
