{
  super(pathElement,JMXExtension.getResourceDescriptionResolver(CommonAttributes.EXPOSE_MODEL + "." + pathElement.getValue()),new ShowModelAdd(auditLoggerInfo,domainName,otherAttributes),new ShowModelRemove(auditLoggerInfo));
  this.auditLoggerInfo=auditLoggerInfo;
  this.domainName=domainName;
}
