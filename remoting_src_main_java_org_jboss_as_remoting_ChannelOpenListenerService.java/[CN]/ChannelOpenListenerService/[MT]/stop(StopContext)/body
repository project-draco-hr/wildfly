{
  if (registration != null) {
    registration.close();
  }
synchronized (channels) {
    for (    ManagementChannel channel : channels) {
      try {
        channel.sendByeBye();
      }
 catch (      IOException e) {
      }
    }
  }
}
