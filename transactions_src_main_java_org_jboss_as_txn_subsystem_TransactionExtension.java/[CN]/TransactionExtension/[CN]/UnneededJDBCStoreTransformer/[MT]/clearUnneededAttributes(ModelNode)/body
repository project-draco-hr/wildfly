{
  ModelNode useJDBC;
  if (!modelNode.hasDefined(TransactionSubsystemRootResourceDefinition.USE_JDBC_STORE.getName()) || ((useJDBC=modelNode.get(TransactionSubsystemRootResourceDefinition.USE_JDBC_STORE.getName())).getType() != ModelType.EXPRESSION && !useJDBC.asBoolean())) {
    for (    AttributeDefinition ad : TransactionSubsystemRootResourceDefinition.attributes_1_2) {
      modelNode.remove(ad.getName());
    }
  }
}
