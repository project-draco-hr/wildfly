{
  if (loaded) {
    throw new IllegalStateException(entity.getEntityName() + "." + getFieldName()+ " single-valued CMR field is already loaded. Check the database for consistency. "+ " current value="+ value+ ", loaded value="+ pk);
  }
  changeValue(pk);
}
