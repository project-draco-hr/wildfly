{
  final ManagementRequestHeader request=super.validateRequest(header);
  if (request.getOperationId() == ModelControllerProtocol.EXECUTE_ASYNC_CLIENT_REQUEST || request.getOperationId() == ModelControllerProtocol.EXECUTE_CLIENT_REQUEST) {
    super.registerActiveOperation(request.getBatchId(),null);
  }
  return request;
}
