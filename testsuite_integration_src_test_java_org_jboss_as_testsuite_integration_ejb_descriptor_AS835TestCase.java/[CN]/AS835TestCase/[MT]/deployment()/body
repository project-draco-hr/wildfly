{
  WebArchive deployment=ShrinkWrap.create(WebArchive.class,"as835.war").addPackage(SimpleStatelessBean.class.getPackage()).addPackage(Naming.class.getPackage()).addAsWebInfResource(new StringAsset(EJB_JAR),"ejb-jar.xml");
  System.out.println(deployment.toString(true));
  return deployment;
}
