{
  LOGGER.debug("Sending STOP command GSSTestServer.");
  final Socket socket=new Socket();
  try {
    socket.connect(new InetSocketAddress(InetAddress.getLocalHost(),GSSTestConstants.PORT),GSSTestConstants.SOCKET_TIMEOUT);
    DataOutputStream dos=new DataOutputStream(socket.getOutputStream());
    dos.writeInt(GSSTestConstants.CMD_STOP);
    dos.flush();
    LOGGER.debug("STOP command sent.");
  }
 catch (  IOException e) {
    LOGGER.error("Problem occured during sending stop command",e);
  }
 finally {
    try {
      socket.close();
    }
 catch (    IOException e) {
      LOGGER.error("Problem occured during closing socket",e);
    }
  }
}
