{
  if (!Util.isValidIdentifier(nodeType)) {
    throw new OperationFormatException("The node type is not a valid identifier '" + nodeType + "' or the format is wrong for prefix '"+ operationStr+ "'");
  }
  validatedNodeType(nodeType);
}
