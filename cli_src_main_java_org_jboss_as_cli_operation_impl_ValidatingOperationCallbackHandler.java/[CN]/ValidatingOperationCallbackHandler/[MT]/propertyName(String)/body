{
  if (!Util.isValidIdentifier(propertyName)) {
    throw new OperationFormatException("Argument name '" + propertyName + "' is not a valid identifier or the format is wrong for the property list.");
  }
  validatedPropertyName(propertyName);
}
