{
  if (!Util.isValidIdentifier(nodeName)) {
    throw new OperationFormatException("The node name is not a valid identifier '" + nodeName + "' or the format is wrong for operation '"+ operationStr+ "'");
  }
  validatedNodeName(nodeName);
}
