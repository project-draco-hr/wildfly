{
  if (!Util.isValidIdentifier(typeOrName)) {
    throw new OperationFormatException("The node type or name is not a valid identifier '" + typeOrName + "' or the format is wrong for opreation '"+ operationStr+ "'");
  }
  validatedNodeTypeOrName(typeOrName);
}
