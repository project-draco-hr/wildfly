{
  pkRef[0]=createPrimaryKeyInstance();
  for (int i=0; i < primaryKeyFields.length; ++i)   parameterIndex=primaryKeyFields[i].loadPrimaryKeyResults(rs,parameterIndex,pkRef);
  return parameterIndex;
}
