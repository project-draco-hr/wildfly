{
  try {
    if (pkField != null) {
      if (primaryKey == null) {
        return null;
      }
      return pkField.get(primaryKey);
    }
 else {
      return primaryKey;
    }
  }
 catch (  Exception e) {
    throw CmpMessages.MESSAGES.errorGettingPk(fieldName,e);
  }
}
