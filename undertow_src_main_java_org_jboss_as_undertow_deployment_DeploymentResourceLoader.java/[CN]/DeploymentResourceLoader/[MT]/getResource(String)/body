{
  try {
    final File child=deploymentRoot.getPhysicalFile();
    File ret=new File(child.getAbsolutePath() + resource);
    if (ret.exists()) {
      return ret;
    }
 else {
      return null;
    }
  }
 catch (  IOException e) {
    return null;
  }
}
