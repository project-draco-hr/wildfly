{
  this.type=type;
  this.processType=type == Type.HOST || type == Type.DOMAIN ? ProcessType.HOST_CONTROLLER : ProcessType.STANDALONE_SERVER;
  runningMode=RunningMode.ADMIN_ONLY;
  testParser=TestParser.create(xmlMapper,type);
}
