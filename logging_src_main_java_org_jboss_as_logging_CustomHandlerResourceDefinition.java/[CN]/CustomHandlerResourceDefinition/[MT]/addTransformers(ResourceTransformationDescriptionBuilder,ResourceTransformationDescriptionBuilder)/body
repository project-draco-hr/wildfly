{
  final ResourceTransformationDescriptionBuilder child=subsystemBuilder.addChildResource(CUSTOM_HANDLE_PATH).getAttributeBuilder().addRejectCheck(RejectAttributeChecker.SIMPLE_EXPRESSIONS,PROPERTIES).end();
  loggingProfileBuilder.discardChildResource(CUSTOM_HANDLE_PATH);
  return registerTransformers(child);
}
