{
  super(PathElement.pathElement(NAME,name),BatchSubsystemDefinition.getResourceDescriptionResolver(NAME,name),addHandler,ReloadRequiredRemoveStepHandler.INSTANCE);
  this.attributes=Collections.unmodifiableCollection(attributes);
}
