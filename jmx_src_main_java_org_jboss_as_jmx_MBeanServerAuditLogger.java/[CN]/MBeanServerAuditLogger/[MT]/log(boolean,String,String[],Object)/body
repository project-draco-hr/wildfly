{
  String domainUUID=null;
  Subject subject=SubjectUtils.getCurrent();
  pluggableMBeanServerImpl.getAuditLogger().logMethodAccess(readOnly,pluggableMBeanServerImpl.isBooting(),getCallerUserId(subject),domainUUID,getSubjectAccessMechanism(subject),getSubjectInetAddress(subject),methodName,methodSignature,methodParams,error);
}
