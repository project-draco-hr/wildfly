{
  controller.start(CONTAINER);
  try {
    client=createClient();
    assertUnpatched(client);
    ageoutHistory(client);
    assertUnpatched(client);
  }
  finally {
    controller.stop(CONTAINER);
  }
}
