{
  final ResourceBundle bundle=getResourceBundle(locale);
  return CommonDescriptions.getSingleParamOnlyOperation(bundle,ProcessReloadHandler.OPERATION_NAME,null,ADMIN_ONLY,ModelType.BOOLEAN,true);
}
