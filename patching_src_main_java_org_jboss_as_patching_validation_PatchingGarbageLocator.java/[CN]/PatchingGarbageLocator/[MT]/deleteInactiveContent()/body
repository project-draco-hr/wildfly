{
  List<File> dirs=getInactiveHistory();
  if (!dirs.isEmpty()) {
    for (    File dir : dirs) {
      deleteDir(dir);
    }
  }
  dirs=getInactiveOverlays();
  if (!dirs.isEmpty()) {
    for (    File dir : dirs) {
      deleteDir(dir);
    }
  }
}
