{
  ServerGroupDeploymentElement toReplace=original.getDeployment(replacedDeploymentName);
  if (toReplace == null)   return null;
  return new ServerGroupDeploymentFullReplaceUpdate(toReplace.getUniqueName(),toReplace.getRuntimeName(),toReplace.getSha1Hash(),uniqueName);
}
