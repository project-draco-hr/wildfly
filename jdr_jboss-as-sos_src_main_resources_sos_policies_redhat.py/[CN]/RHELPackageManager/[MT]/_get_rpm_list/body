def _get_rpm_list(self):
    pkg_list = shell_out(['rpm', '-qa', '--queryformat', '%{NAME}|%{VERSION}\\n']).splitlines()
    self._rpms = {}
    for pkg in pkg_list:
        (name, version) = pkg.split('|')
        self._rpms[name] = {'name': name, 'version': version, }
