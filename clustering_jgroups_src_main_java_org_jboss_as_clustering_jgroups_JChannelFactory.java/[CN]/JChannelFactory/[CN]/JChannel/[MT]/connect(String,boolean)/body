{
  ConnectTask connectTask=new ConnectTask(){
    @Override public void connect() throws ChannelException {
      JChannel.super.connect(clusterName,useFlushIfPresent);
    }
  }
;
  this.connect(connectTask);
}
