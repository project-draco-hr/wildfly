{
  return "<subsystem xmlns=\"urn:jboss:domain:jca:1.0\">" + "    <archive-validation enabled=\"false\" />" + "    <bean-validation enabled=\"false\" />"+ "    <default-workmanager>"+ "        <short-running-threads blocking=\"true\">"+ "                <core-threads count=\"10\" per-cpu=\"20\"/>"+ "                <queue-length count=\"10\" per-cpu=\"20\"/>"+ "                <max-threads count=\"10\" per-cpu=\"20\"/>"+ "                <keepalive-time time=\"10\" unit=\"seconds\"/>"+ "        </short-running-threads>"+ "        <long-running-threads blocking=\"true\">"+ "                <core-threads count=\"10\" per-cpu=\"20\"/>"+ "                <queue-length count=\"10\" per-cpu=\"20\"/>"+ "                <max-threads count=\"10\" per-cpu=\"20\"/>"+ "                <keepalive-time time=\"10\" unit=\"seconds\"/>"+ "        </long-running-threads>"+ "    </default-workmanager>"+ "</subsystem>";
}
