{
  if (planId == null)   throw ControllerClientLogger.ROOT_LOGGER.nullVar("planId");
  if (actionResults == null)   throw ControllerClientLogger.ROOT_LOGGER.nullVar("actionResults");
  this.planId=planId;
  this.actionResults=actionResults;
}
