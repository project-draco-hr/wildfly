{
  if (file == null) {
    return;
  }
  for (  File f : file.listFiles()) {
    recursiveDelete(f);
  }
  if (!file.delete()) {
    throw UndertowMessages.MESSAGES.couldNotDeleteTempFile(file);
  }
}
