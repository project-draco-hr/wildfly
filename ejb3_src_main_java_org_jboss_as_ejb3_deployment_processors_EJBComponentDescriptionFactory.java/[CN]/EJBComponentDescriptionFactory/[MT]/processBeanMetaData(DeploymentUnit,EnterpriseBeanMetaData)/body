{
  if (enterpriseBeanMetaData instanceof SessionBeanMetaData)   processSessionBeanMetaData(deploymentUnit,(SessionBeanMetaData)enterpriseBeanMetaData);
 else   throw new IllegalArgumentException("Unable to process " + enterpriseBeanMetaData);
}
