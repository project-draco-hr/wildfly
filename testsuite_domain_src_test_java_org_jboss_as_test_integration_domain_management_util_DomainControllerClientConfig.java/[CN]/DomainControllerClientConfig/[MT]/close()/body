{
  if (destroyExecutor) {
    executorService.shutdown();
  }
  if (endpoint != null)   try {
    endpoint.close();
  }
 catch (  IOException e) {
  }
  if (destroyExecutor) {
    executorService.shutdownNow();
  }
}
