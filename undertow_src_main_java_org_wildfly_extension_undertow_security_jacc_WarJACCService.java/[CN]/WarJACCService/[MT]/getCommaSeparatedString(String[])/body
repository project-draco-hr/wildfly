{
  int len=str.length;
  Arrays.sort(str);
  StringBuilder buf=new StringBuilder();
  for (int i=0; i < len; i++) {
    if (i > 0)     buf.append(",");
    buf.append(str[i]);
  }
  return buf.toString();
}
