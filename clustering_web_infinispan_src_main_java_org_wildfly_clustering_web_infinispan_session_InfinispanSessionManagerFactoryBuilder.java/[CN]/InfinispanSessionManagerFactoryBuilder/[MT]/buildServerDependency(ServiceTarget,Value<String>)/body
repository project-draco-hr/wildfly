{
  RegistryEntryProvider<String,Void> provider=new RegistryEntryProvider<String,Void>(){
    @Override public String getKey(){
      return instanceId.getValue();
    }
    @Override public Void getValue(){
      return null;
    }
  }
;
  return target.addService(ServiceName.JBOSS.append("clustering","registry",DEFAULT_CACHE_CONTAINER,CacheContainer.DEFAULT_CACHE_ALIAS,"entry"),new ValueService<>(new ImmediateValue<>(provider))).setInitialMode(ServiceController.Mode.ON_DEMAND);
}
