{
  if (contained.getRepository() != delegateFor.getRepository())   throw JacORBLogger.ROOT_LOGGER.wrongInterfaceRepository();
  if (contMap.get(name) != null)   throw JacORBLogger.ROOT_LOGGER.duplicateRepositoryName();
  cont.add(contained);
  contMap.put(name,contained);
}
