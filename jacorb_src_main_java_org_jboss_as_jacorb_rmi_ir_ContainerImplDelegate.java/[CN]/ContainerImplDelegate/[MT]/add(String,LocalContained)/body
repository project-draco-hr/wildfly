{
  if (contained.getRepository() != delegateFor.getRepository())   throw JacORBMessages.MESSAGES.wrongInterfaceRepository();
  if (contMap.get(name) != null)   throw JacORBMessages.MESSAGES.duplicateRepositoryName();
  cont.add(contained);
  contMap.put(name,contained);
}
