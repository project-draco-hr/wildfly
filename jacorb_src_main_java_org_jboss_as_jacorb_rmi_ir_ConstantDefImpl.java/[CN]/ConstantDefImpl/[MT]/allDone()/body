{
  type_def=IDLTypeImpl.getIDLType(typeCode,repository);
  if (type_def == null)   logger.debug("Got type_def: [NULL]");
 else   logger.debug("Got type_def: [" + type_def.toString() + "]");
  getReference();
}
