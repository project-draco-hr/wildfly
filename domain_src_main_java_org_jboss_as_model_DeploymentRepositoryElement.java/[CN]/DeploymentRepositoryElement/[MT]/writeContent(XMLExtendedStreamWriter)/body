{
  streamWriter.writeAttribute(Attribute.PATH.getLocalName(),path);
  if (interval != 0)   streamWriter.writeAttribute(Attribute.SCAN_INTERVAL.getLocalName(),String.valueOf(interval));
  if (!enabled)   streamWriter.writeAttribute(Attribute.SCAN_ENABLED.getLocalName(),"false");
  streamWriter.writeEndElement();
}
