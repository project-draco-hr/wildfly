{
  try {
    if (executeOperation1 == null) {
      executeOperation1=childFirstClassLoaderServices.getClass().getMethod("executeOperation",childFirstClassLoader.loadClass(ModelNode.class.getName()),InputStream[].class);
    }
    return converter.convertModelNodeFromChildCl(executeOperation1.invoke(childFirstClassLoaderServices,converter.convertModelNodeToChildCl(operation),inputStreams));
  }
 catch (  Exception e) {
    unwrapInvocationTargetRuntimeException(e);
    throw new RuntimeException(e);
  }
}
