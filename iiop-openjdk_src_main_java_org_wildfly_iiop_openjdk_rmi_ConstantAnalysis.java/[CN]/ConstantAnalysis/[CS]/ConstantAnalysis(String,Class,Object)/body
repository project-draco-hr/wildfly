{
  super(javaName);
  if (type == Void.TYPE || !type.isPrimitive() && type != java.lang.String.class)   throw IIOPLogger.ROOT_LOGGER.badConstantType(type.getName());
  this.type=type;
  this.value=value;
}
