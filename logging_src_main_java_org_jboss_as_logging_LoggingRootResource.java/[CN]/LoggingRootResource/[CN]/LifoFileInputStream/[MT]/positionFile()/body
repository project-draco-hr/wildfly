{
  end=start;
  if (end == 0) {
    end=-1;
    start=-1;
    pos=-1;
    return;
  }
  long filePointer=start - 1;
  while (true) {
    filePointer--;
    if (filePointer < 0) {
      break;
    }
    raf.seek(filePointer);
    final byte readByte=raf.readByte();
    if (readByte == '\n' && filePointer != (len - 1)) {
      break;
    }
  }
  start=filePointer + 1;
  pos=start;
}
