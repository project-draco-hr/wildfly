{
  final InjectedValue<T> value=new InjectedValue<T>();
  return context.getServiceTarget().addService(serviceName,new ValueService<T>(value)).addDependency(dependencyServiceName,targetClass,value).setInitialMode(ServiceController.Mode.ON_DEMAND).install();
}
