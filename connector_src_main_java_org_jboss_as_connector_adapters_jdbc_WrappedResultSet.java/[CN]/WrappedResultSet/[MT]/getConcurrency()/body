{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getConcurrency()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.getConcurrency();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
