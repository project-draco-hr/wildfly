{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getClob(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,i);
    return resultSet.getClob(i);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
