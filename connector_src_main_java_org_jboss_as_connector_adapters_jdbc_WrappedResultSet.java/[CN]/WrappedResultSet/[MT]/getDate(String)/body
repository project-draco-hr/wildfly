{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getDate(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName);
    return resultSet.getDate(columnName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
