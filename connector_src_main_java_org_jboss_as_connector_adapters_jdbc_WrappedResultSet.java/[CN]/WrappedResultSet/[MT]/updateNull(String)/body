{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateNull(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName);
    resultSet.updateNull(columnName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
