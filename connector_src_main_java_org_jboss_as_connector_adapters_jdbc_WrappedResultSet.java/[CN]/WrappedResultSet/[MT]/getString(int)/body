{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getString(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnIndex);
    return resultSet.getString(columnIndex);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
