{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getFetchSize()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.getFetchSize();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
