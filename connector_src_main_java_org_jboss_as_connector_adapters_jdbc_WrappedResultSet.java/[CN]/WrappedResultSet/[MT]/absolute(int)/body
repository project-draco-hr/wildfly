{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] absolute(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,row);
    return resultSet.absolute(row);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
