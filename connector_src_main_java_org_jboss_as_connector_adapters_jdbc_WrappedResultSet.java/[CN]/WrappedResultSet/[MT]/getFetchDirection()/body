{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getFetchDirection()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.getFetchDirection();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
