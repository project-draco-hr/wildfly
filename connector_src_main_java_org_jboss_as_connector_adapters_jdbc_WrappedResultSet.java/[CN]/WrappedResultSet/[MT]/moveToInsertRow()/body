{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] moveToInsertRow()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    resultSet.moveToInsertRow();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
