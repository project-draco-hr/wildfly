{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getURL(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName);
    return resultSet.getURL(columnName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
