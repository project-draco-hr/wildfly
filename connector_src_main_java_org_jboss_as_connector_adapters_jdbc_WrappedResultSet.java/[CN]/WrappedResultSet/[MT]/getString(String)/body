{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getString(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName);
    return resultSet.getString(columnName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
