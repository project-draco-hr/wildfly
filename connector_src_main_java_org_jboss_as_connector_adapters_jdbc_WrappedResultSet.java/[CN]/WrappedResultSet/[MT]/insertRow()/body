{
  statement.lock();
  try {
    checkTransaction();
    try {
      if (spy)       spyLogger.debugf("%s [%s] insertRow()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
      resultSet.insertRow();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    statement.unlock();
  }
}
