{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] rowInserted()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.rowInserted();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
