{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] moveToCurrentRow()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    resultSet.moveToCurrentRow();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
