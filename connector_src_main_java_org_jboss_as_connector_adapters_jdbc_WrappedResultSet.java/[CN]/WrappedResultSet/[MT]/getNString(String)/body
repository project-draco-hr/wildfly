{
  ResultSet resultSet=getUnderlyingResultSet();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getNString(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnLabel);
    return resultSet.getNString(columnLabel);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
