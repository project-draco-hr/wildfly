{
  if (closed.get())   return;
  if (spy)   spyLogger.debugf("%s [%s] close()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
  closed.set(true);
  statement.unregisterResultSet(this);
  internalClose();
}
