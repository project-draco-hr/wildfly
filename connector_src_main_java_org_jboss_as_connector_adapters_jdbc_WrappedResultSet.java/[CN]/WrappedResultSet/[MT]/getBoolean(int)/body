{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getBoolean(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnIndex);
    return resultSet.getBoolean(columnIndex);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
