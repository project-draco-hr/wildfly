{
  ResultSet resultSet=getUnderlyingResultSet();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getRowId(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnLabel);
    return resultSet.getRowId(columnLabel);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
