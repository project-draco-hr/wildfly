{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getLong(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName);
    return resultSet.getLong(columnName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
