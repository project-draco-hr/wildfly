{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getArray(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,i);
    return resultSet.getArray(i);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
