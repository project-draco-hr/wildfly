{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateRef(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName,x);
    resultSet.updateRef(columnName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
