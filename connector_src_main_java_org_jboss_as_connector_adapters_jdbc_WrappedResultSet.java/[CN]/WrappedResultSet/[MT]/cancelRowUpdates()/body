{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] cancelRowUpdates()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    resultSet.cancelRowUpdates();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
