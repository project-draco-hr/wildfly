{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] isAfterLast()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.isAfterLast();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
