{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] isLast()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.isLast();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
