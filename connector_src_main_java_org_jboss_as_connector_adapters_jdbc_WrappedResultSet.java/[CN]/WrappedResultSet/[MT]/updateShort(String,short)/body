{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateShort(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName,x);
    resultSet.updateShort(columnName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
