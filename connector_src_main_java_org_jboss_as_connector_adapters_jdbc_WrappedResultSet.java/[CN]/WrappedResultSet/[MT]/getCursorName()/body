{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getCursorName()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.getCursorName();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
