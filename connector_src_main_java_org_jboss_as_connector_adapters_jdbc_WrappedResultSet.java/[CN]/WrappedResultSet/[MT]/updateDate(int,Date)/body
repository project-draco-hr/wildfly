{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateDate(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnIndex,x);
    resultSet.updateDate(columnIndex,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
