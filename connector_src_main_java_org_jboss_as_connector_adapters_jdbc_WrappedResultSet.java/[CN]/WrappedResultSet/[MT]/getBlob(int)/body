{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getBlob(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,i);
    return resultSet.getBlob(i);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
