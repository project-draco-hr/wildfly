{
  if (spy)   spyLogger.debugf("%s [%s] isClosed()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
  ResultSet resultSet=getWrappedObject();
  if (resultSet == null)   return true;
  try {
    return resultSet.isClosed();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
