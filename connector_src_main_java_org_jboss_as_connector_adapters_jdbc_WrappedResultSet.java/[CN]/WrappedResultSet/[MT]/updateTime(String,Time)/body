{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateTime(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName,x);
    resultSet.updateTime(columnName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
