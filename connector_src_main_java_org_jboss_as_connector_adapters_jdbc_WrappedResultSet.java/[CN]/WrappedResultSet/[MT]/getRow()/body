{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getRow()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.getRow();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
