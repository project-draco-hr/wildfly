{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateBytes(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName,Arrays.toString(x));
    resultSet.updateBytes(columnName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
