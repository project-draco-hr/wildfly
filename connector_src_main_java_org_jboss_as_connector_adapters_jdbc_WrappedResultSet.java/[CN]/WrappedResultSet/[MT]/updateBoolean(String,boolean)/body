{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateBoolean(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName,x);
    resultSet.updateBoolean(columnName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
