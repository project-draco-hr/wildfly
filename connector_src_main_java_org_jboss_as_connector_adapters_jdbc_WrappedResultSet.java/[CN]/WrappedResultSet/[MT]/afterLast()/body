{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] afterLast()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    resultSet.afterLast();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
