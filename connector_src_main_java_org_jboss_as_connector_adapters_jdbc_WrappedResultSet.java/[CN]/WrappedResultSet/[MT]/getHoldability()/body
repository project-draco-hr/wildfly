{
  ResultSet resultSet=getUnderlyingResultSet();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getHoldability()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.getHoldability();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
