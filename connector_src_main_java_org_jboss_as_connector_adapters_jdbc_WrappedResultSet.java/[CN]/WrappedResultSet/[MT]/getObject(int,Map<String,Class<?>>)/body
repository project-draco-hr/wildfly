{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getObject(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,i,map);
    return resultSet.getObject(i,map);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
