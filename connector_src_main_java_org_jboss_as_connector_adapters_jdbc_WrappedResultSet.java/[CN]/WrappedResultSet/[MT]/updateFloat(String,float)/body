{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateFloat(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName,x);
    resultSet.updateFloat(columnName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
