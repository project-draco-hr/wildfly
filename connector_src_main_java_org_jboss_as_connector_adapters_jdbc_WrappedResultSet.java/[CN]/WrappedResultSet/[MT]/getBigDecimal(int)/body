{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getBigDecimal(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnIndex);
    return resultSet.getBigDecimal(columnIndex);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
