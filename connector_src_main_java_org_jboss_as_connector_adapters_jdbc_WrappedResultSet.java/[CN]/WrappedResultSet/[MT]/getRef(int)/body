{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getRef(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,i);
    return resultSet.getRef(i);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
