{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getBlob(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,colName);
    return resultSet.getBlob(colName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
