{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getWarnings()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.getWarnings();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
