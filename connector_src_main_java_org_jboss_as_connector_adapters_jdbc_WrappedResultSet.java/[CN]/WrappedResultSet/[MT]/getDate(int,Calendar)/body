{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getDate(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnIndex,cal);
    return resultSet.getDate(columnIndex,cal);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
