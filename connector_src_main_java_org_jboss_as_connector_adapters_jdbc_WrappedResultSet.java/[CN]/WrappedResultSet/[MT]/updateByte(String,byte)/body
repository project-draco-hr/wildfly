{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateByte(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName,x);
    resultSet.updateByte(columnName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
