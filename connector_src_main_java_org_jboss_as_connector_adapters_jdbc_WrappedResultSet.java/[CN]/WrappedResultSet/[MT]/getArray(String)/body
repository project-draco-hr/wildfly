{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getArray(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,colName);
    return resultSet.getArray(colName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
