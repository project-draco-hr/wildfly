{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] rowUpdated()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.rowUpdated();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
