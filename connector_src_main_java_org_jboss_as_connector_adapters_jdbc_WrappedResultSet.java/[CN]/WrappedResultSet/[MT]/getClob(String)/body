{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getClob(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,colName);
    return resultSet.getClob(colName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
