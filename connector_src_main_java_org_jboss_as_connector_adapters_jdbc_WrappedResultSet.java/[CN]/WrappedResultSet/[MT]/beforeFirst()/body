{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] beforeFirst()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    resultSet.beforeFirst();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
