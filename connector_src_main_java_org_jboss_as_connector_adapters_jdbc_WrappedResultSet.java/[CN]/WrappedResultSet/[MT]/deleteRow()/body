{
  statement.lock();
  try {
    checkTransaction();
    try {
      if (spy)       spyLogger.debugf("%s [%s] deleteRow()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
      resultSet.deleteRow();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    statement.unlock();
  }
}
