{
  ResultSet resultSet=getUnderlyingResultSet();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getNString(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnIndex);
    return resultSet.getNString(columnIndex);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
