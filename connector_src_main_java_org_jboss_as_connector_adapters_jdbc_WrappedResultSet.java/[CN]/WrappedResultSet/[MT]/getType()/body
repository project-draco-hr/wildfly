{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getType()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.getType();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
