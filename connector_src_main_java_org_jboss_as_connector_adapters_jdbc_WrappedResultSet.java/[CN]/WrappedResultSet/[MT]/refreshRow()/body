{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] refreshRow()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    resultSet.refreshRow();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
