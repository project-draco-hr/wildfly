{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateTime(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnIndex,x);
    resultSet.updateTime(columnIndex,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
