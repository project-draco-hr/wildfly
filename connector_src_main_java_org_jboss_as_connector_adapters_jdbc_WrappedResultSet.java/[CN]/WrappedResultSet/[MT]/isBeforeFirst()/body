{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] isBeforeFirst()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.isBeforeFirst();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
