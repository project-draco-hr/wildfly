{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] updateDouble(%s, %s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName,x);
    resultSet.updateDouble(columnName,x);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
