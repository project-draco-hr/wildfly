{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] clearWarnings()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    resultSet.clearWarnings();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
