{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] getMetaData()",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET);
    return resultSet.getMetaData();
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
