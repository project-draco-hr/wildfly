{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] findColumn(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,columnName);
    return resultSet.findColumn(columnName);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
