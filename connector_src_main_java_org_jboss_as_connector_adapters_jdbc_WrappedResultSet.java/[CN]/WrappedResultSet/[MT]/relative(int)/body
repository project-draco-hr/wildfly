{
  checkState();
  try {
    if (spy)     spyLogger.debugf("%s [%s] relative(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_RESULTSET,rows);
    return resultSet.relative(rows);
  }
 catch (  Throwable t) {
    throw checkException(t);
  }
}
