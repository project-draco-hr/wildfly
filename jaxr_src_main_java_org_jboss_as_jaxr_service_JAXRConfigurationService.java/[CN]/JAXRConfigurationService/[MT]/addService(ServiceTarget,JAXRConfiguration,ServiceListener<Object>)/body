{
  JAXRConfigurationService service=new JAXRConfigurationService(config);
  ServiceBuilder<?> builder=target.addService(JAXRConfiguration.SERVICE_NAME,service);
  builder.addListener(listeners);
  return builder.install();
}
