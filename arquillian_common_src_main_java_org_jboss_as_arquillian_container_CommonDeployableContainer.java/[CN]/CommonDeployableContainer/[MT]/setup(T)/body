{
  containerConfig=config;
  ModelControllerClient modelControllerClient=ModelControllerClient.Factory.create(config.getManagementAddress(),config.getManagementPort());
  managementClient=new ManagementClient(modelControllerClient);
  archiveDeployerInst.set(new ArchiveDeployer(ServerDeploymentManager.Factory.create(modelControllerClient)));
}
