{
  PathAddress addr=!domainModelImpl.isMaster() && address.size() > 0 && address.getElement(0).getKey().equals(HOST) ? address.subAddress(1) : address;
  super.addProxyResultToMainResult(addr,mainResult,proxyResult);
}
