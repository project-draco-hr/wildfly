{
  boolean empty=false;
  FileInputStream fis=null;
  assertTrue(file.exists());
  try {
    fis=new FileInputStream(file);
    empty=fis.read() == -1;
  }
  finally {
    if (fis != null) {
      fis.close();
    }
  }
  return empty;
}
