{
  ModelNode value=parse("\" \"");
  assertNotNull(value);
  assertEquals(ModelType.STRING,value.getType());
  assertEquals(" ",value.asString());
  value=parse(" \" \"");
  assertNotNull(value);
  assertEquals(ModelType.STRING,value.getType());
  assertEquals(" ",value.asString());
  value=parse(" \" \\\"\"");
  assertNotNull(value);
  assertEquals(ModelType.STRING,value.getType());
  assertEquals(" \"",value.asString());
}
