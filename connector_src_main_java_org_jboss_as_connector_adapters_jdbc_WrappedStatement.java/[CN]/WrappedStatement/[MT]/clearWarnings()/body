{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] clearWarnings()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      s.clearWarnings();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
