{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] getQueryTimeout()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      return s.getQueryTimeout();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
