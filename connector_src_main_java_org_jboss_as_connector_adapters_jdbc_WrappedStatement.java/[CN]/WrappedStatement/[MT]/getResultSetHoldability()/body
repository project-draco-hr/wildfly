{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] getResultSetHoldability()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      return s.getResultSetHoldability();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
