{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] addBatch(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT,sql);
      s.addBatch(sql);
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
