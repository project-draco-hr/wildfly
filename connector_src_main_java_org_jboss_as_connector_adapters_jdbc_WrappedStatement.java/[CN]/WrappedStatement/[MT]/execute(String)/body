{
  lock();
  try {
    checkTransaction();
    try {
      checkConfiguredQueryTimeout();
      if (spy)       spyLogger.debugf("%s [%s] execute(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT,sql);
      return s.execute(sql);
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
