{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] getResultSetConcurrency()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      return s.getResultSetConcurrency();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
