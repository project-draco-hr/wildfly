{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] getMaxRows()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      return s.getMaxRows();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
