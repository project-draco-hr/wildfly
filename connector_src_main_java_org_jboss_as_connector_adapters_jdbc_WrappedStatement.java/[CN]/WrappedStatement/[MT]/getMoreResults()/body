{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] getMoreResults()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      return s.getMoreResults();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
