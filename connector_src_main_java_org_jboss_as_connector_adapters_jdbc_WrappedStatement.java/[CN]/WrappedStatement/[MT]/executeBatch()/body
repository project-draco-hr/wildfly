{
  lock();
  try {
    checkState();
    try {
      checkConfiguredQueryTimeout();
      if (spy)       spyLogger.debugf("%s [%s] executeBatch()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      return s.executeBatch();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
