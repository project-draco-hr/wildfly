{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] getResultSetType()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      return s.getResultSetType();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
