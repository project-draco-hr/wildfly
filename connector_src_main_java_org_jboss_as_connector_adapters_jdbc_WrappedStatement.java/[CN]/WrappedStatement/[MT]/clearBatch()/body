{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] clearBatch()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      s.clearBatch();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
