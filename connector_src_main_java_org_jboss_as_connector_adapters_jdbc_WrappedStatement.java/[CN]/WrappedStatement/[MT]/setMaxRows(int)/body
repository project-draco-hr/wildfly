{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] setMaxRows(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT,max);
      s.setMaxRows(max);
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
