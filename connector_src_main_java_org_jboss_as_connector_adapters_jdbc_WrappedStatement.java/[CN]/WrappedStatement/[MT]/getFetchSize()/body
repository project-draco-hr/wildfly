{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] getFetchSize()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      return s.getFetchSize();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
