{
  lock();
  try {
    checkState();
    try {
      if (spy)       spyLogger.debugf("%s [%s] getGeneratedKeys()",jndiName,Constants.SPY_LOGGER_PREFIX_STATEMENT);
      ResultSet resultSet=s.getGeneratedKeys();
      return registerResultSet(resultSet);
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
