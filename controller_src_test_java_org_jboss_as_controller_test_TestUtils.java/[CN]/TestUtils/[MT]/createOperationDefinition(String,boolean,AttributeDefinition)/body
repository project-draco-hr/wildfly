{
  SimpleOperationDefinitionBuilder builder=new SimpleOperationDefinitionBuilder(name,new NonResolvingResourceDescriptionResolver()).setParameters(parameters);
  if (runtimeOnly) {
    builder.setRuntimeOnly();
  }
  return builder.build();
}
