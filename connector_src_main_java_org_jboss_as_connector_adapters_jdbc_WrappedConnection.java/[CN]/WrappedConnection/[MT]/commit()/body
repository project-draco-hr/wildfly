{
  lock();
  try {
    checkTransaction();
    if (spy)     spyLogger.debugf("%s [%s] commit()",jndiName,Constants.SPY_LOGGER_PREFIX_CONNECTION);
    mc.jdbcCommit();
  }
  finally {
    unlock();
  }
}
