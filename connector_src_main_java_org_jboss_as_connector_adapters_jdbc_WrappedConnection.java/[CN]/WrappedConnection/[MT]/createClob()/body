{
  lock();
  try {
    Connection c=getUnderlyingConnection();
    try {
      if (spy)       spyLogger.debugf("%s [%s] createClob()",jndiName,Constants.SPY_LOGGER_PREFIX_CONNECTION);
      return c.createClob();
    }
 catch (    Throwable t) {
      throw checkException(t);
    }
  }
  finally {
    unlock();
  }
}
