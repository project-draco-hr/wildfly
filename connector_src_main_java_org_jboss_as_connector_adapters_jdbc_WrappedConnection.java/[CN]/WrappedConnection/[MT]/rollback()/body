{
  lock();
  try {
    checkTransaction();
    if (spy)     spyLogger.debugf("%s [%s] rollback()",jndiName,Constants.SPY_LOGGER_PREFIX_CONNECTION);
    mc.jdbcRollback();
  }
  finally {
    unlock();
  }
}
