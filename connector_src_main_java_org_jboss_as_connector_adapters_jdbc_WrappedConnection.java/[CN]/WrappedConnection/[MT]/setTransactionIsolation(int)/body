{
  lock();
  try {
    checkStatus();
    if (spy)     spyLogger.debugf("%s [%s] setTransactionIsolation(%s)",jndiName,Constants.SPY_LOGGER_PREFIX_CONNECTION,isolationLevel);
    mc.setJdbcTransactionIsolation(isolationLevel);
  }
  finally {
    unlock();
  }
}
