{
  return !cancelled && !rolledBack && domainFailure == null && (hostFailures == null || hostFailures.size() == 0) && (serverFailures == null || serverFailures.size() == 0) && (serverCancellations == null || serverCancellations.size() == 0) && (serverTimeouts == null || serverTimeouts.size() == 0) && (serverRollbacks == null || serverRollbacks.size() == 0);
}
