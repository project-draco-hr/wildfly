{
  if (cancelled || rolledBack || domainFailure != null || (hostFailures != null && hostFailures.size() > 0))   throw new IllegalStateException("Cannot add server results to an update that was not successfully applied to the domain");
}
