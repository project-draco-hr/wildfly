{
  if (!value.isDefined()) {
    if (!nullable)     throw MESSAGES.nullNotAllowed(parameterName);
  }
 else {
    boolean matched=false;
    if (strictType) {
      matched=validTypes.contains(value.getType());
    }
 else {
      for (      ModelType validType : validTypes) {
        if (matches(value,validType)) {
          matched=true;
          break;
        }
      }
    }
    if (!matched)     throw MESSAGES.incorrectType(parameterName,validTypes,value.getType());
  }
}
