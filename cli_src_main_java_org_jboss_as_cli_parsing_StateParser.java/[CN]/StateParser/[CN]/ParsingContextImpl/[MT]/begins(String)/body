{
  if (location + seq.length() < input.length()) {
    int i=0;
    while (i < seq.length()) {
      if (input.charAt(location + i) != seq.charAt(i++)) {
        return false;
      }
    }
    return true;
  }
  return false;
}
