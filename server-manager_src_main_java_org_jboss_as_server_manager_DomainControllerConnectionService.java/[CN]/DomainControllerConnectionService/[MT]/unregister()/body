{
  try {
    DomainControllerClientProtocol.OutgoingCommand.UNREGISTER.execute(this,DomainControllerConnectionService.this.socketOut);
  }
 catch (  Throwable t) {
    throw new RuntimeException("Failed to unregister from domain controller",t);
  }
}
