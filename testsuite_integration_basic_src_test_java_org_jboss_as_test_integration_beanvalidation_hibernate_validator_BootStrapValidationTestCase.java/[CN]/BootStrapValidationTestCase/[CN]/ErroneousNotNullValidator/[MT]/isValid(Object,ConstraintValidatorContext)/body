{
  if (obj != null) {
    String var=((String)obj);
    if (var.length() > 0 && var.length() < 10 && var.matches("^[0-9]+$")) {
      return true;
    }
 else {
      return false;
    }
  }
 else {
    return false;
  }
}
