{
  final List<ResourceRoot> entries=new ArrayList<ResourceRoot>();
  entries.add(new ResourceRoot(deploymentRoot.getChild(WEB_INF_CLASSES).getName(),deploymentRoot.getChild(WEB_INF_CLASSES),null,false));
  createWebInfLibResources(deploymentRoot,entries);
  return entries;
}
