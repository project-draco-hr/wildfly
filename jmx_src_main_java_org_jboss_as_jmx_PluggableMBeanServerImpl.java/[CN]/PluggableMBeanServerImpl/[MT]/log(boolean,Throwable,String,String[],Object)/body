{
  String domainUUID=null;
  Subject subject=SubjectUtils.getCurrent();
  auditLogger.logMethodAccess(readOnly,getCallerUserId(subject),domainUUID,getSubjectAccessMechanism(subject),getSubjectInetAddress(subject),methodName,methodSignature,methodParams,error);
}
