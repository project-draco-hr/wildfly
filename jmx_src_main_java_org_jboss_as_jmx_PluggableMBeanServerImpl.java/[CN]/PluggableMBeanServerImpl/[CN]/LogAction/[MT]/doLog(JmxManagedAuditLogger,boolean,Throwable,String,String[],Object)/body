{
  Subject subject=SubjectUtils.getCurrent();
  if (auditLogger != null) {
    auditLogger.logMethodAccess(readOnly,getCallerUserId(subject),null,getSubjectAccessMechanism(subject),getSubjectInetAddress(subject),methodName,methodSignature,methodParams,error);
  }
}
