{
  Subject subject=SubjectUtils.getCurrent();
  if (auditLogger != null) {
    auditLogger.logJmxMethodAccess(readOnly,getCallerUserId(subject),null,null,getSubjectInetAddress(subject),methodName,methodSignature,methodParams,error);
  }
}
