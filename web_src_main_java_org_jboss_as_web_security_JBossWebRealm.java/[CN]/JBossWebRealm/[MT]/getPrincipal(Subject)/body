{
  Principal principal=null;
  if (subject != null) {
    Set<Principal> principals=subject.getPrincipals();
    if (principals != null && !principals.isEmpty()) {
      for (      Principal p : principals) {
        principal=p;
        if (!(principal instanceof Group))         break;
      }
    }
  }
  return principal;
}
