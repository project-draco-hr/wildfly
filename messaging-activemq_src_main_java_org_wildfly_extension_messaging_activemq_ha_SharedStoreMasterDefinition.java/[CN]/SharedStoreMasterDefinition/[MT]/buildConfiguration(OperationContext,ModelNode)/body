{
  return new SharedStoreMasterPolicyConfiguration().setFailbackDelay(FAILBACK_DELAY.resolveModelAttribute(context,model).asLong()).setFailoverOnServerShutdown(FAILOVER_ON_SERVER_SHUTDOWN.resolveModelAttribute(context,model).asBoolean());
}
