{
  final ModelNode root=ROOT.clone();
  if (recursive) {
    root.get(CHILDREN,EXTENSION,MODEL_DESCRIPTION).set("TODO");
    root.get(CHILDREN,PATH,MODEL_DESCRIPTION).set(PathDescription.getNamedPathDescription());
    root.get(CHILDREN,PROFILE,MODEL_DESCRIPTION).set("TODO");
    root.get(CHILDREN,INTERFACE,MODEL_DESCRIPTION).set("TODO");
    root.get(CHILDREN,SOCKET_BINDING_GROUP,MODEL_DESCRIPTION).set("TODO");
    root.get(CHILDREN,SYSTEM_PROPERTY,MODEL_DESCRIPTION).set("TODO");
    root.get(CHILDREN,DEPLOYMENT,MODEL_DESCRIPTION).set("TODO");
    root.get(CHILDREN,SERVER_GROUP,MODEL_DESCRIPTION).set("TODO");
    root.get(CHILDREN,HOST,MODEL_DESCRIPTION).set(HostRootDescription.getDescription(recursive));
    root.get(CHILDREN,SERVER,MODEL_DESCRIPTION).set("TODO");
  }
  return root;
}
