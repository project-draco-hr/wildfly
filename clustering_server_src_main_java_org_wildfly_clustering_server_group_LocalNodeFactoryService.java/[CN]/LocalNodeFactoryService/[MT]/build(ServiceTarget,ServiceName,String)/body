{
  LocalNodeFactoryService service=new LocalNodeFactoryService(cluster);
  return target.addService(name,service).addDependency(ServerEnvironmentService.SERVICE_NAME,ServerEnvironment.class,service.environment);
}
