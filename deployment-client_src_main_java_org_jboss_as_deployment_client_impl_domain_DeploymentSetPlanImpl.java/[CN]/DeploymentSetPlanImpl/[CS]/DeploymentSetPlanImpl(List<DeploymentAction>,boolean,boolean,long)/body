{
  if (actions == null)   throw new IllegalArgumentException("actions is null");
  this.deploymentActions.addAll(actions);
  this.rollback=rollback;
  this.shutdown=shutdown;
  this.gracefulShutdownPeriod=gracefulTimeout;
}
