{
synchronized (this) {
    if (!start) {
      return;
    }
    stopped=true;
    final OutputStream stream=commandStream;
    StreamUtils.writeString(stream,Command.SHUTDOWN + "\n");
    stream.flush();
    commandStream.close();
  }
}
