{
  if (!ProcessManagerMaster.SERVER_MANAGER_PROCESS_NAME.equals(processName))   throw new IllegalStateException("Attempt to send SHUTDOWN_SERVERS on a ManagedProcess that is not " + ProcessManagerMaster.SERVER_MANAGER_PROCESS_NAME + ": "+ processName);
}
