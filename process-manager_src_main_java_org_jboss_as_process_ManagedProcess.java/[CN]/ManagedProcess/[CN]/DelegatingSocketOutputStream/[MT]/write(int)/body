{
  if (current == null) {
synchronized (this) {
      if (current == null) {
        if (realOut == null) {
          bytesOut=new ByteArrayOutputStream();
          current=bytesOut;
        }
 else {
          current=realOut;
        }
      }
    }
  }
  current.write(b);
}
