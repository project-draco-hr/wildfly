{
synchronized (this) {
    if (realOut == null) {
      throw new IllegalStateException("The socket output stream was already closed " + processName);
    }
    safeClose(realOut);
    realOut=null;
    current=null;
  }
}
