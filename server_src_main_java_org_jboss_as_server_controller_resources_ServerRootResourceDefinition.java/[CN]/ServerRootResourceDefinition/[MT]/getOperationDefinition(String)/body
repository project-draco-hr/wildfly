{
  return new SimpleOperationDefinitionBuilder(name,ServerDescriptions.getResourceDescriptionResolver(RUNNING_SERVER)).setParameters(BLOCKING).setReplyType(ModelType.STRING).withFlags(OperationEntry.Flag.HOST_CONTROLLER_ONLY,OperationEntry.Flag.RUNTIME_ONLY).build();
}
