{
  InjectedValue<ServerEnvironment> environment=new InjectedValue<>();
  return target.addService(name,new SimpleCacheFactoryService<K,V>(environment,timeout)).addDependency(ServerEnvironmentService.SERVICE_NAME,ServerEnvironment.class,environment);
}
