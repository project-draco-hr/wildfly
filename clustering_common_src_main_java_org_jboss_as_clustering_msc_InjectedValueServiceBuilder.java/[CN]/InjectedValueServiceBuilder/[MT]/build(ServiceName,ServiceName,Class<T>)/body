{
  InjectedValue<T> value=new InjectedValue<>();
  return this.target.addService(name,new ValueService<>(value)).addDependency(targetName,targetClass,value).setInitialMode(ServiceController.Mode.ON_DEMAND);
}
