{
  try {
    ServerMessage sm=messages.poll(10,TimeUnit.SECONDS);
    if (sm == null)     throw new RuntimeException("Read timed out");
    return sm;
  }
 catch (  InterruptedException e) {
    throw new RuntimeException(e);
  }
}
