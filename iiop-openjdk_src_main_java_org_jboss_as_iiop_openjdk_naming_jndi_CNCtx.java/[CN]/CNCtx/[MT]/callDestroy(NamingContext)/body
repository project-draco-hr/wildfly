{
  if (_nc == null)   throw IIOPMessages.MESSAGES.notANamingContext(nc.toString());
  try {
    nc.destroy();
  }
 catch (  Exception e) {
    throw org.jboss.as.iiop.openjdk.naming.jndi.ExceptionMapper.mapException(e,this,null);
  }
}
