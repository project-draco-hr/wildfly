{
  try {
    super.start(context);
  }
 catch (  StartException e) {
    error=e;
    latch.countDown();
    throw e;
  }
catch (  Throwable t) {
    error=t;
    latch.countDown();
    throw new StartException(t);
  }
}
