{
  super(processType,runningModeControl,persister,new ControlledProcessState(true),DESC_PROVIDER,null,ExpressionResolver.DEFAULT);
  this.persister=persister;
  this.transformerRegistry=transformerRegistry;
  this.validateOps=validateOps;
}
